# Architecture Decision Records

The design canon (docs 08–29) carries ADR-001…ADR-U37 inline. This
directory holds the records made *in the implementation* after the canon
was written — most importantly the ones that **reverse a previously
recorded non-goal**, because a scope reversal that lives only in a commit
message is indistinguishable from scope creep.

The change-control protocol (§10 Part 11) applies: the newest accepted
decision wins and must be recorded. These files are that record.

| ADR | Decision | Reverses |
|---|---|---|
| [029](029-mcp-transport-partial.md) | MCP is the real transport for the L0 read-only tool surface; L1/L2 stay in-process | narrows the "in-process by ADR'd mapping" compromise |
| [030](030-multi-tenant-server.md) | One `serve` process may front several isolated workspaces | "Multi-tenant SaaS" (README out-of-scope) — the server half only |
| [031](031-policy-armed-automation.md) | Merge and deploy execution become possible, but only when a human arms a policy file | "Auto-merge to main. Auto-deploy to production." (README out-of-scope, §08.1.8) |
| [032](032-no-framework-spending-cap.md) | Spend is measured and reported, never gated — budget limits live at the provider that does the billing | the monthly spending cap (v0.65.0–v0.66.0) |
| [033](033-withdraw-weekly-attention-axis.md) | The weekly maintenance-hours kill axis is withdrawn; no scheduled loop asks a human for a number, and a non-zero exit is now always a failure | PRD outcome O-L1 and its kill criterion (v0.50.0–v0.80.0), and the alert path's `attention` exemption |
| [034](034-the-bench-is-a-watched-loop.md) | The product-bench series becomes a third cadence loop with a per-loop timeout, and leaves cron for launchd | "the series is already collected weekly" — a claim about a scheduler nobody watched |
| [035](035-an-unmeasured-case-is-not-a-zero.md) | A rate averages only over cases that produced its denominator, the denominator travels into the series and the alert, and a run that could not measure a case exits 3 | averaging a crashed case in as `0.0`; the probe frame's discarded 4xx body; and part of `2bb4808`'s reasoning |
| [036](036-a-hang-must-describe-itself.md) | A timeout carries what the process printed, kills the whole process group, and keeps the crashed case's workspace; a module that blocks on import is rejected from a parse, in every profile (scope corrected in v0.86.0) | nothing — it answers ADR-035's open bullet |
| [038](038-the-thresholds-that-must-differ.md) | `ROLLBACK_SEVERITIES` is named and pinned as a strict subset of the set that prompts a fix, "clean" gets one definition derived from the `Verdict` enum, and the founder tally stops reading a rejection as an approval | nothing — it completes ADR-037 by fixing the *class* rather than the instance |
| [039](039-one-issue-is-one-finding.md) | The leader folds repeats of one issue across files into one finding (carrying every site), the repair cap is named and reports what it dropped, a static-analysis hit on a test file is a note rather than a blocker, a rejection records which voter made it, and both claim gates compile one shared superlative vocabulary | nothing — it fixes the class behind run 13's clean-review rate, where ADR-037 and ADR-038 fixed instances |
| [037](037-block-and-repair-are-one-threshold.md) | The repair pass filters by the leader's own `ACTIONABLE_SEVERITIES` instead of a second hard-coded list, so MEDIUM can no longer block a verdict that nothing will ever try to fix; every non-clean verdict records what it objected to, and a result file names the build that produced it | nothing — it explains run 14's 38% clean-review rate |
| [040](040-a-result-is-not-an-exit-code.md) | A loop's last *result* is alertable separately from whether it ran, a poor-but-complete run is a finding that fails nothing, the floors keep one definition, run-over-run movement is stated without inventing a threshold, and `product-bench --notify` lets any run — however started — report itself through the one delivery path | nothing — it fixes the two reasons the alert channel was silent about runs 12–15 |
| [041](041-an-empty-answer-is-not-a-verdict.md) | An empty spec is the loudest complaint in the revision loop instead of the quietest, the spec stage asks whether its response was cut off like every other writer stage does, a truncated response gets its own block reason, and the ledger records `stop_reason` on every call | nothing — it fixes the two blocked specs of run 15 and makes the next occurrence answerable from the ledger |
| [042](042-a-failure-must-arrive-as-a-fact.md) | A build failure's one-line cause is selected from pytest's own summary and assertion lines instead of head-sliced off the front of its banner art, keeping the test's name and the real comparison, with one definition of the clip | nothing — it makes run 15's third finding readable without re-running the preserved workspace |
| [043](043-a-case-is-measured-or-it-is-not.md) | Whether a bench case was measured is one per-case decision that every rate reads, a case that ran and produced nothing scores a real 0.0 instead of vanishing from the denominator, a blocked plan carries its reason into the result file and its parser's message into the revision prompt, and a rejection caused by voters that never answered says so | ADR-035's implementation, not its principle — "a case that ran and built nothing still scores a real 0.0" was already its text; the build rate's comparability breaks at run 17 |
| [044](044-a-repair-lands-whole-or-not-at-all.md) | The test write-guard judges the batch instead of one file, so an assert hoisted into a shared helper reads as moved rather than deleted and the consolidation refactor run 16's reviewers kept demanding can actually be performed; a partly-refused repair is applied to none instead of committing its half; each of the repair pass's seven failure paths names itself; a rolled-back repair no longer sets the verdict for a tree it discarded; and a case with a rejected task keeps its workspace | nothing in ADR-037/038 — `ROLLBACK_SEVERITIES` is untouched. §13.29.5's write-lock is narrowed for the first time, only along the axis that made it self-defeating |
| [045](045-a-requirement-has-a-name.md) | Every acceptance criterion in the product gets a permanent id in one derived, append-only ledger keyed on text rather than position, kept current by the build rather than by a person; a planner is shown the promises its request touches, and is told in words that the ones it was not shown still exist | nothing — it adds the level above the spec file, where `ears.py`, the SCR channel and `covers` all stop |
| [046](046-a-new-requirement-is-reconciled.md) | A new request is classified against the promises it touches before it is planned: a duplicate refuses the build in both modes and exits 0, a contradiction stops for a person and under `--yes` raises an SCR it never approves, an unreadable check is never read as a clean one, and only an explicit `--replace` retires anything — after the build succeeds | nothing in ADR-U02 — it routes into the SCR channel. It narrows `--yes` for the first time: "do not stop for me about the *plan*", not authorization to retire a promise or to rebuild one |
| [047](047-the-product-has-a-constitution.md) | Section 4 of the FDR — what the founder ruled out — is derived into `product/constitution.yaml` and shown to every planner: deterministic extraction (no model call), append-only ids, reconciled per origin so a silent feature FDR cannot repeal the founding document, rendered whole with an announced cap, and gating nothing — the newest request always wins | nothing. It reads a section that was already written and already ignored |
| [048](048-the-founder-sees-the-shape-of-the-work.md) | Two views over the requirement ledger, both re-derived rather than believed: `avs roadmap` turns a paragraph into ordered increments and re-checks the remainder against the ledger after each build (a step is done only when the reconciler says so; unchecked is reported, never counted as pending), and `avs requirements` shows what the product promises with a per-checkpoint baseline behind it. Also records the defect this surfaced: ADR-046's retrieval tokenizer required an ASCII letter, so the gate was *inert* — never firing, never erroring — for every Chinese request, which is every benchmark case | nothing. The granularity rule is enforced, not relaxed — what changes is who does the work of satisfying it |
| [049](049-the-refusal-is-measured-on-its-own-axis.md) | The one thing the product bench could not see was a refusal that did not happen: a gate that never fires scores exactly like one that fires correctly. Cases now declare an `axis` — the four real cases that produce the headline series stay on `build`, and a new Chinese increment case scores a separate **gate rate** against expectations written *before* the run (duplicate → nothing built, contradiction → an unapproved SCR, clean addition → built). A raised SCR outranks the status in both directions, because under `--yes` a contradiction proceeds and reads `completed` exactly like an inert gate | `cases_total` is no longer every case file in the directory but the build-axis count — the narrowing that stops a fifth case silently redefining the run-13..17 series |
| [050](050-one-tokenizer.md) | ADR-048 recorded one inert gate; the audit found the same shape in **seven** tokenizers, four of which had independently learned that Chinese has no spaces and three of which had not. `textsim` scored two IDENTICAL Chinese strings at 0.0 and all three of its callers compare `>= threshold`, so it failed *open* — nothing was ever a duplicate, everything was always novel. One module now (`lexicon`), imported everywhere; CJK detected by Unicode name rather than a range literal that misses the extension blocks; stopwords and length floors stay per-caller because they are genuinely different jobs (ADR-038), and the length floor never touches CJK. Guarded by an AST walk over `src/`, and the guard is itself pinned against the five patterns that shipped blind — a check that cannot fire is the defect it was written to prevent | nothing in ADR-038: what merges is the lexical layer under both stopword lists, which was one job written seven times. Four call sites change behaviour beyond the CJK fix, each recorded |
| [051](051-a-second-path-is-not-a-weaker-one.md) | The sibling of ADR-038's class: not one concept with two definitions, but **one control with two call paths**, where the second silently does less. `--parallel` builds were merged and recorded `built` with `review_verdict=None` — the exact hole `retry-task` shipped with, surviving because the wave loop is hand-written rather than routed through `_attempt_task`; they now run the same `review_and_repair`, and the merge carries its bookkeeping inside it because adding a review made `checkout -- .` reachable. ADR-U03's taint collapse and the risk ceiling were wired only into the MCP transport, which is *not* the default: nothing was exploitable, because every voter tool is L0 — the guarantee held because the table was short, not because anything checked. Both are now enforced in-process against the same tables, `build_toolbox` passes the same arguments to both branches, and ADR-038's subset relation is checked at import rather than only in the suite | nothing — both halves restore a guarantee the system already claimed |
| [052](052-a-measurement-is-bought-once.md) | Run 17 measured one case over 3438s of real spend, lost its account, and left nothing reusable: `save_summary` runs once at the end, so a kill — including ADR-036's 8h group-kill, of which run 16 already used 2.97h — destroys every finished case. A measured case is now banked before the next one starts, and `--resume` reuses it only when case digest, `avs_version` and provider all still match, because `autopilot._todo_and_skipped` already established that "skipping work is only safe when we can say what work it was". A resumed row says so everywhere it appears. Separately, "one case never kills the bench" is narrowed for the first time: a dead account is not a case failure, and the untried cases are recorded under one reason instead of rediscovering it four times at 0.3s each — detected both by a table of provider wording and by two consecutive identical failures, the second because the first stops firing the day the provider rewords its errors | nothing — it extends to the bench case the rule already kept for build tasks, deploy super-steps and workspace bookkeeping. Cross-build reuse is refused rather than offered, so the optimisation cannot reintroduce ADR-049's confound |
| [053](053-a-rate-over-no-cases.md) | Pricing a cheaper bench run — the increment case alone, ~2h instead of ~5h — found that its empty build axis made `_avg([])` return `0.0`, so a run that never asked whether anything builds would have entered the capability ledger **below floor**, one run from firing a criterion that asks a human to consider killing the project; `cadence` would have reported it as a flat "build 0%, probes 0%", since the "over N of M" qualifier needs `measured < total` and `0 < 0` is false. `BenchSummary`'s three headline rates become `float \| None` like `gate_rate` already was, the file writes `null` rather than omitting the key, and both readers check for it explicitly instead of relying on `float(None)` raising into a handler meant for malformed files. Floors, streak and `below_floor` untouched: what changes is which runs are eligible to be judged. Also names the build in every bench claim and records that run 16 (v0.93.0) is the newest reading and not a reading of this build | nothing — it enforces ADR-035 one level above where ADR-035 enforced it. ADR-051's shape: the rule held on the path everyone takes and was absent from the path nobody had taken |
| [054](054-the-criterion-must-survive-being-read.md) | Asked to close whatever remained after ADR-053, the check this time was to RUN the reporting commands rather than read them — and `avs bench-criterion` crashed with `NameError: streak_state` on ten orphaned lines from the implementation it replaced. Unreachable in exactly one case, because a fired criterion exits at 3 above them: the command failed on every healthy run and worked only when the project was in trouble, across eleven recorded runs, because **no test invoked it**. Two more defects in the same twenty lines: an aborted attempt counted as a run (`save_summary` writes `aborted:` above the rates precisely because "four cases failed" and "this run never got to ask them" look identical as percentages, and the one reader where it decides something never looked — so an exhausted billing account could advance a streak that asks a human to consider killing the project), and the ledger's stated "oldest first, by filename" invariant broke when ADR-052 added `aborted-*.yaml` beside `result-*.yaml` and `a` sorted before `r`. Excluded is not invisible: aborts are still named, with the command that would finish them. `cadence` now prints the build each reading came from, since its `state` column measures staleness in days and days is a proxy that breaks when releases outpace the cadence | nothing. ADR-051's shape three more times: a writer added something and a reader that had documented its assumptions was not updated with it |

## Format

Each record states: context, the decision, what it reverses and why that
reversal is defensible, what stays out, and the mechanism that keeps the
new capability bounded. A record without a *mechanism* section is an
opinion, not a decision.
