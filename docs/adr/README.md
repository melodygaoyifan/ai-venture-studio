# Architecture Decision Records

The design canon (docs 08–29) carries ADR-001…ADR-U37 inline. This
directory holds the records made *in the implementation* after the canon
was written — most importantly the ones that **reverse a previously
recorded non-goal**, because a scope reversal that lives only in a commit
message is indistinguishable from scope creep.

The change-control protocol (§10 Part 11) applies: the newest accepted
decision wins and must be recorded. These files are that record.

| ADR | Decision | Reverses |
|---|---|---|
| [029](029-mcp-transport-partial.md) | MCP is the real transport for the L0 read-only tool surface; L1/L2 stay in-process | narrows the "in-process by ADR'd mapping" compromise |
| [030](030-multi-tenant-server.md) | One `serve` process may front several isolated workspaces | "Multi-tenant SaaS" (README out-of-scope) — the server half only |
| [031](031-policy-armed-automation.md) | Merge and deploy execution become possible, but only when a human arms a policy file | "Auto-merge to main. Auto-deploy to production." (README out-of-scope, §08.1.8) |
| [032](032-no-framework-spending-cap.md) | Spend is measured and reported, never gated — budget limits live at the provider that does the billing | the monthly spending cap (v0.65.0–v0.66.0) |
| [033](033-withdraw-weekly-attention-axis.md) | The weekly maintenance-hours kill axis is withdrawn; no scheduled loop asks a human for a number, and a non-zero exit is now always a failure | PRD outcome O-L1 and its kill criterion (v0.50.0–v0.80.0), and the alert path's `attention` exemption |
| [034](034-the-bench-is-a-watched-loop.md) | The product-bench series becomes a third cadence loop with a per-loop timeout, and leaves cron for launchd | "the series is already collected weekly" — a claim about a scheduler nobody watched |
| [035](035-an-unmeasured-case-is-not-a-zero.md) | A rate averages only over cases that produced its denominator, the denominator travels into the series and the alert, and a run that could not measure a case exits 3 | averaging a crashed case in as `0.0`; the probe frame's discarded 4xx body; and part of `2bb4808`'s reasoning |
| [036](036-a-hang-must-describe-itself.md) | A timeout carries what the process printed, kills the whole process group, and keeps the crashed case's workspace; a module that blocks on import is rejected from a parse, in every profile (scope corrected in v0.86.0) | nothing — it answers ADR-035's open bullet |
| [038](038-the-thresholds-that-must-differ.md) | `ROLLBACK_SEVERITIES` is named and pinned as a strict subset of the set that prompts a fix, "clean" gets one definition derived from the `Verdict` enum, and the founder tally stops reading a rejection as an approval | nothing — it completes ADR-037 by fixing the *class* rather than the instance |
| [039](039-one-issue-is-one-finding.md) | The leader folds repeats of one issue across files into one finding (carrying every site), the repair cap is named and reports what it dropped, a static-analysis hit on a test file is a note rather than a blocker, a rejection records which voter made it, and both claim gates compile one shared superlative vocabulary | nothing — it fixes the class behind run 13's clean-review rate, where ADR-037 and ADR-038 fixed instances |
| [037](037-block-and-repair-are-one-threshold.md) | The repair pass filters by the leader's own `ACTIONABLE_SEVERITIES` instead of a second hard-coded list, so MEDIUM can no longer block a verdict that nothing will ever try to fix; every non-clean verdict records what it objected to, and a result file names the build that produced it | nothing — it explains run 14's 38% clean-review rate |
| [040](040-a-result-is-not-an-exit-code.md) | A loop's last *result* is alertable separately from whether it ran, a poor-but-complete run is a finding that fails nothing, the floors keep one definition, run-over-run movement is stated without inventing a threshold, and `product-bench --notify` lets any run — however started — report itself through the one delivery path | nothing — it fixes the two reasons the alert channel was silent about runs 12–15 |
| [041](041-an-empty-answer-is-not-a-verdict.md) | An empty spec is the loudest complaint in the revision loop instead of the quietest, the spec stage asks whether its response was cut off like every other writer stage does, a truncated response gets its own block reason, and the ledger records `stop_reason` on every call | nothing — it fixes the two blocked specs of run 15 and makes the next occurrence answerable from the ledger |
| [042](042-a-failure-must-arrive-as-a-fact.md) | A build failure's one-line cause is selected from pytest's own summary and assertion lines instead of head-sliced off the front of its banner art, keeping the test's name and the real comparison, with one definition of the clip | nothing — it makes run 15's third finding readable without re-running the preserved workspace |
| [043](043-a-case-is-measured-or-it-is-not.md) | Whether a bench case was measured is one per-case decision that every rate reads, a case that ran and produced nothing scores a real 0.0 instead of vanishing from the denominator, a blocked plan carries its reason into the result file and its parser's message into the revision prompt, and a rejection caused by voters that never answered says so | ADR-035's implementation, not its principle — "a case that ran and built nothing still scores a real 0.0" was already its text; the build rate's comparability breaks at run 17 |
| [044](044-a-repair-lands-whole-or-not-at-all.md) | The test write-guard judges the batch instead of one file, so an assert hoisted into a shared helper reads as moved rather than deleted and the consolidation refactor run 16's reviewers kept demanding can actually be performed; a partly-refused repair is applied to none instead of committing its half; each of the repair pass's seven failure paths names itself; a rolled-back repair no longer sets the verdict for a tree it discarded; and a case with a rejected task keeps its workspace | nothing in ADR-037/038 — `ROLLBACK_SEVERITIES` is untouched. §13.29.5's write-lock is narrowed for the first time, only along the axis that made it self-defeating |
| [045](045-a-requirement-has-a-name.md) | Every acceptance criterion in the product gets a permanent id in one derived, append-only ledger keyed on text rather than position, kept current by the build rather than by a person; a planner is shown the promises its request touches, and is told in words that the ones it was not shown still exist | nothing — it adds the level above the spec file, where `ears.py`, the SCR channel and `covers` all stop |
| [046](046-a-new-requirement-is-reconciled.md) | A new request is classified against the promises it touches before it is planned: a duplicate refuses the build in both modes and exits 0, a contradiction stops for a person and under `--yes` raises an SCR it never approves, an unreadable check is never read as a clean one, and only an explicit `--replace` retires anything — after the build succeeds | nothing in ADR-U02 — it routes into the SCR channel. It narrows `--yes` for the first time: "do not stop for me about the *plan*", not authorization to retire a promise or to rebuild one |
| [047](047-the-product-has-a-constitution.md) | Section 4 of the FDR — what the founder ruled out — is derived into `product/constitution.yaml` and shown to every planner: deterministic extraction (no model call), append-only ids, reconciled per origin so a silent feature FDR cannot repeal the founding document, rendered whole with an announced cap, and gating nothing — the newest request always wins | nothing. It reads a section that was already written and already ignored |
| [048](048-the-founder-sees-the-shape-of-the-work.md) | Two views over the requirement ledger, both re-derived rather than believed: `avs roadmap` turns a paragraph into ordered increments and re-checks the remainder against the ledger after each build (a step is done only when the reconciler says so; unchecked is reported, never counted as pending), and `avs requirements` shows what the product promises with a per-checkpoint baseline behind it. Also records the defect this surfaced: ADR-046's retrieval tokenizer required an ASCII letter, so the gate was *inert* — never firing, never erroring — for every Chinese request, which is every benchmark case | nothing. The granularity rule is enforced, not relaxed — what changes is who does the work of satisfying it |
| [049](049-the-refusal-is-measured-on-its-own-axis.md) | The one thing the product bench could not see was a refusal that did not happen: a gate that never fires scores exactly like one that fires correctly. Cases now declare an `axis` — the four real cases that produce the headline series stay on `build`, and a new Chinese increment case scores a separate **gate rate** against expectations written *before* the run (duplicate → nothing built, contradiction → an unapproved SCR, clean addition → built). A raised SCR outranks the status in both directions, because under `--yes` a contradiction proceeds and reads `completed` exactly like an inert gate | `cases_total` is no longer every case file in the directory but the build-axis count — the narrowing that stops a fifth case silently redefining the run-13..17 series |
| [050](050-one-tokenizer.md) | ADR-048 recorded one inert gate; the audit found the same shape in **seven** tokenizers, four of which had independently learned that Chinese has no spaces and three of which had not. `textsim` scored two IDENTICAL Chinese strings at 0.0 and all three of its callers compare `>= threshold`, so it failed *open* — nothing was ever a duplicate, everything was always novel. One module now (`lexicon`), imported everywhere; CJK detected by Unicode name rather than a range literal that misses the extension blocks; stopwords and length floors stay per-caller because they are genuinely different jobs (ADR-038), and the length floor never touches CJK. Guarded by an AST walk over `src/`, and the guard is itself pinned against the five patterns that shipped blind — a check that cannot fire is the defect it was written to prevent | nothing in ADR-038: what merges is the lexical layer under both stopword lists, which was one job written seven times. Four call sites change behaviour beyond the CJK fix, each recorded |
| [051](051-a-second-path-is-not-a-weaker-one.md) | The sibling of ADR-038's class: not one concept with two definitions, but **one control with two call paths**, where the second silently does less. `--parallel` builds were merged and recorded `built` with `review_verdict=None` — the exact hole `retry-task` shipped with, surviving because the wave loop is hand-written rather than routed through `_attempt_task`; they now run the same `review_and_repair`, and the merge carries its bookkeeping inside it because adding a review made `checkout -- .` reachable. ADR-U03's taint collapse and the risk ceiling were wired only into the MCP transport, which is *not* the default: nothing was exploitable, because every voter tool is L0 — the guarantee held because the table was short, not because anything checked. Both are now enforced in-process against the same tables, `build_toolbox` passes the same arguments to both branches, and ADR-038's subset relation is checked at import rather than only in the suite | nothing — both halves restore a guarantee the system already claimed |
| [052](052-a-measurement-is-bought-once.md) | Run 17 measured one case over 3438s of real spend, lost its account, and left nothing reusable: `save_summary` runs once at the end, so a kill — including ADR-036's 8h group-kill, of which run 16 already used 2.97h — destroys every finished case. A measured case is now banked before the next one starts, and `--resume` reuses it only when case digest, `avs_version` and provider all still match, because `autopilot._todo_and_skipped` already established that "skipping work is only safe when we can say what work it was". A resumed row says so everywhere it appears. Separately, "one case never kills the bench" is narrowed for the first time: a dead account is not a case failure, and the untried cases are recorded under one reason instead of rediscovering it four times at 0.3s each — detected both by a table of provider wording and by two consecutive identical failures, the second because the first stops firing the day the provider rewords its errors | nothing — it extends to the bench case the rule already kept for build tasks, deploy super-steps and workspace bookkeeping. Cross-build reuse is refused rather than offered, so the optimisation cannot reintroduce ADR-049's confound |
| [053](053-a-rate-over-no-cases.md) | Pricing a cheaper bench run — the increment case alone, ~2h instead of ~5h — found that its empty build axis made `_avg([])` return `0.0`, so a run that never asked whether anything builds would have entered the capability ledger **below floor**, one run from firing a criterion that asks a human to consider killing the project; `cadence` would have reported it as a flat "build 0%, probes 0%", since the "over N of M" qualifier needs `measured < total` and `0 < 0` is false. `BenchSummary`'s three headline rates become `float \| None` like `gate_rate` already was, the file writes `null` rather than omitting the key, and both readers check for it explicitly instead of relying on `float(None)` raising into a handler meant for malformed files. Floors, streak and `below_floor` untouched: what changes is which runs are eligible to be judged. Also names the build in every bench claim and records that run 16 (v0.93.0) is the newest reading and not a reading of this build | nothing — it enforces ADR-035 one level above where ADR-035 enforced it. ADR-051's shape: the rule held on the path everyone takes and was absent from the path nobody had taken |
| [054](054-the-criterion-must-survive-being-read.md) | Asked to close whatever remained after ADR-053, the check this time was to RUN the reporting commands rather than read them — and `avs bench-criterion` crashed with `NameError: streak_state` on ten orphaned lines from the implementation it replaced. Unreachable in exactly one case, because a fired criterion exits at 3 above them: the command failed on every healthy run and worked only when the project was in trouble, across eleven recorded runs, because **no test invoked it**. Two more defects in the same twenty lines: an aborted attempt counted as a run (`save_summary` writes `aborted:` above the rates precisely because "four cases failed" and "this run never got to ask them" look identical as percentages, and the one reader where it decides something never looked — so an exhausted billing account could advance a streak that asks a human to consider killing the project), and the ledger's stated "oldest first, by filename" invariant broke when ADR-052 added `aborted-*.yaml` beside `result-*.yaml` and `a` sorted before `r`. Excluded is not invisible: aborts are still named, with the command that would finish them. `cadence` now prints the build each reading came from, since its `state` column measures staleness in days and days is a proxy that breaks when releases outpace the cadence | nothing. ADR-051's shape three more times: a writer added something and a reader that had documented its assumptions was not updated with it |
| [055](055-a-name-that-resolves-nowhere.md) | ADR-054's lesson generalised: running the command beat reading it, but **both sample the same thing** — the paths something actually takes. A test proves the code it *calls* works and says nothing about code no test calls, which is precisely what `streak_state` was, so neither 2229 hermetic tests nor a careful audit could have reached it. `ruff check` reads every line whether or not anything runs it; the project had no linter at all, and the first run found the identical defect twice more. `MCPHost.__init__` annotated `taint: "TaintGuard \| None"` with nothing importing `TaintGuard` — lazy under `from __future__ import annotations`, so it never raised and the declared type on the risk-tier RBAC boundary was checked by no one (`get_type_hints()` raises `NameError` on the deployed v0.103.0). And the stage-floor guard formats `{floor.name}` into an assertion message where `floor` is out of scope, so the day it caught a regression it would have died with `NameError` instead of naming which stage ran where. `F` rules only — pinned by a test, because a gate that reformats 300 files is one people learn to scroll past — and run by both workflows, because `publish.yml` claiming in prose to run "the same gate as ci.yml" while assembling it by hand is ADR-051's shape aimed at the release path. The `TaintGuard` import is deliberately NOT under `TYPE_CHECKING`: that satisfies the linter and leaves `get_type_hints()` raising the same error — the defect preserved, the report silenced | nothing. It corrects one specific claim: the CI comment "the suite IS the gate" was true about whether the system works and was never true about whether the code means anything |
| [056](056-a-reading-that-cannot-name-its-instrument.md) | Run 17 blocked on credit, so: what can be validated without it? The obvious answer is `avs product-bench --provider mock` — free, offline, a documented option — and checking it *before* running it (ADR-054's habit) found that a simulated run dual-wrote into `benchmarks/results/` and was **byte-identical in shape to a real reading**: same rates, same denominators, same `avs_version`, no field naming the instrument. `_scan` globs `result-*.yaml` and had nothing to filter on, so `evaluate()` counted it in the streak, `movement()` diffed the next real run against it, and `cadence` reported it to a human — a build rate that is a fact about a regex table feeding the launch PRD's only kill criterion, whose output is a person being asked whether to kill the project. Depending only on how the mock scored it could mask two bad runs or advance the streak toward that question. Nobody had run it: safe by accident. Now `provider:` is recorded on **every** run (a field that appears only when something is wrong is one nobody looks for), a simulated run stays in `.mas/` and out of the tracked ledger, and `_scan` refuses *and names* one that arrives there anyway — two layers, each silent about the other's case. Absence of the key reads as REAL, because the eleven recorded results predate the field and the other guess would delete the whole series from the criterion's view. `SIMULATED_PROVIDERS` lives in the registry alone, pinned by a test | nothing. It narrows what may enter `benchmarks/results/` a second time along a new axis, after ADR-054 narrowed it once |
| [057](057-the-bench-could-never-say-what-it-cost.md) | Every product-bench result records `duration_s` and nothing about money — run 17's row says a case took 3438 seconds and died, and no file in the series says what those seconds bought. That number decides whether to rerun, whether to add a case, whether a nightly cadence is affordable, and the only way to get it was to open the provider console and guess which charges belonged to which run. **The data was never missing.** `spend.record()` meters every call; `autopilot` flushes the ledger into the workspace root, so rows are attributed by construction; `run_case` then deletes that tree in a `finally`. Complete, correct, and inside the directory being deleted — ADR-051's shape again, cost metering read back by `build`/`autopilot`/`graph`/`studio`/`gepa`/`smoke`/`cli` and not by the one path that spends the most. Now read on the way out, on the success path and on the crash path (where it matters most), rendered on the CLI and in the alert whose own docstring said this run costs "real money" and then reported only the hours. `usd` is `None` and never `0.0` when unpriced (ADR-053 applied to money); a partial total announces itself as a FLOOR; a resumed row contributes nothing; and prices come from the OPERATOR's repo, never from the `mkdtemp` workspace that has never held a price table and never will | nothing. It fills a hole in ADR-032's transparency story; no cap, no refusal, no prices in code |
| [058](058-the-run-knew-and-the-record-did-not.md) | Six findings from inspecting run 17's abort and run 18's workspaces, and they are one defect six times: the system established a fact and the place that needed it did not receive it. **Preserved workspaces** were keyed by case name and `rmtree`'d first, so run 18's opening act was deleting run 17's forensics — the run whose forensics were the reason to look; now keyed by run stamp, pruned by age. **Run 17's abort** was excluded from the capability series by its FILENAME alone, one of two guards engaged, so a copy under a `result-` name re-enters as build-100% over 1 of 5. **Gate 2** writes why it downgraded an APPROVE into `leader.summary`, which nothing read — the one rejection that knows its exact cause arrived worst-explained, and where a voter was also blocked the row said "this is what rejected the task" about a voter that had not. **`_tool_request` returned `None`** for both "not a tool request" and "a tool request that would not parse", so an investigation turn went to the verdict parser, raised, and burned every retry: twelve of run 18's seventeen blocked votes, all one YAML rule (`glob: **/*.py` unquoted opens an alias) — the review ceiling HISTORY blamed on severity calibration for three runs. **`assess_fdr`** applied the first-FDR bar to follow-up FDRs, reading everything unmentioned as missing rather than unchanged; all three of run 18's follow-ups stopped at intake, so the increment axis's 0% is not a reading of the gate. **`lane_check`** named the forbidden arrangement and never a legal one; `03-groupbuy-auto` was told the same sentence three times and built nothing, while case 01 had already solved the identical collision by hoisting the shared file | nothing — six repairs to channels that already existed and carried less than their sender knew |

| [059](059-the-check-that-cannot-fire.md) | The one finding ADR-058 named and left open. `lane_check` compares tasks in DIFFERENT lanes, so a single-lane plan cannot collide however many tasks share a file — the check that protects parallelism is silent exactly when there is none left to protect, and a planner can clear a real collision by merging the two lanes. Run 18 scored both sides: cases 02 and 04 collapsed to one lane and passed unexamined (04 with three tasks all expecting `app/candidates.py`), while case 03 kept two honest lanes, could not resolve the collision in three attempts, and built nothing. ADR-058's own MERGE remedy made the dodge easier to find. Closed by DESCRIBING rather than refusing: `lane_advisories` names the arrangement, its wave count and the hoist that undoes it, and is delivered three ways that cannot force a revision — a `minor` critic issue, the lane COUNT in plan.md, and revision feedback only when a revision is already happening. `status` is computed from `dag_issues` alone and a test reads the source to keep advisories out of it, because a bar the planner cannot clear is what killed case 03. MERGE now states that it costs parallelism; `CaseResult.lanes` keeps the arrangement after the workspace is gone | nothing — ADR-058 deferred this deliberately and recorded the deferral; this is that decision being made |

| [060](060-asking-the-question-without-paying-for-a-run.md) | ADR-058's six findings had one shape — a component established a fact and the reader that needed it never got it — and every one was found by hand, after a $67.88 run had already paid to expose the symptom. The shape is mechanical, so it is asked mechanically now: an AST sweep over every record class under `src/`, against a deliberately generous notion of a reader (attribute load, subscript, `.get`/`.pop`, or the name inside any string in any tracked file, because a field rendered into a report is read by a person). 1.2 seconds, and five defects no run had ever surfaced — two of them `BuildResult` fields **ADR-058's own fix walked past while adding three of their neighbours**. The sharp one: `wireup_issues` is computed only on a SUCCESSFUL build, so the outcome the record had no way to state was the one that looks best and is worst — built, tests green, nothing imports it, and every reader saw `status: built` and stopped. Also: which policy file authorised a merge or a deploy, absent from the only durable record of it; `CascadePolicy.escalate_on` shipping `low_confidence` for its whole life with no confidence input to judge it by; Gate PL3 asserting zero preflight findings instead of counting them; and a kill criterion that never stated the floor it judged against. `escalate_on` needed a decision rather than a wire-up — low_confidence is opt-in and opting in without a confidence raises, since blanket-escalation turns the cascade off while looking like it is on. The allowlist is where judgment lives: 24 fields, each with a written reason, and a test that fails an excuse shorter than four words | nothing. It removes one class of finding from the bench's job; it cannot find a fact that is wrong, only one that reaches nobody |
| [061](061-one-failure-one-column.md) | The same question aimed at the result files instead of the source, at the per-outcome level `HISTORY.md` never reached — no API spend, out of a run already read twice. Run 18's `03-groupbuy-auto` was blocked at planning, built 0 of 0, and was charged a hard 0.0 in the probe column too: one failure moving two of three headline numbers, dragging the run's probe rate to 75% when three cases were probed and all three passed. The correct rule was written verbatim on `clean_review_rate` one property below the one that got it wrong. Also: a probe that cannot parse is OUR defect, and `test_every_probe_compiles` cannot reach the probes `probegen` writes during a run — the larger population, since case 03 declares none of its own; `run_probe` now compiles every probe and returns `harness_fault`, which stays in the row and leaves the denominator. Run 18's and run 16's recorded rates are NOT re-scored, and a test pins run 18's 0.75 against exactly that temptation. The trap on the way, caught by the suite rather than foreseen: a run where EVERY case failed now writes a null probe rate, and `bench_criterion` skipped any run with one — the worst reading the series can produce would have become the one the kill criterion could not see | half of the run-16 correction in HISTORY.md. Its build half stands; its probe half made the probe column a second printing of the build column. What it got right is kept and is the more important half — the exclusion must be per-case and it must be PRINTED, which is what `no_probe_reading` and the narrower denominator on the CLI and in the alert exist to satisfy || [062](062-an-ignore-is-a-decision.md) | The same question aimed at the LINTER CONFIG. ADR-055 drew `select = ["F"]` deliberately narrow — "if a later change widens `select`, this is the test that asks whether the widening was deliberate" — and 54 releases later it was still asking one question while CLAUDE.md carried three invariants stated in prose with no mechanism at all (no runtime asserts, no silent swallows, absolute executable paths). Widened to the fourteen families about code that MISBEHAVES; the cosmetic ones stay out and a test now pins that boundary instead of a count. It landed at zero findings, not a backlog — the condition on the decision, since a gate people are told to work through later is the gate ADR-055 refused. Three defects confirmed live in the INSTALLED v0.109.0 first: a desync probe that returned `passed=True` for a client that stopped producing (`zip` reads the common prefix, and a stream that stops has no divergence in it — the most complete desync available, green); `check_substantiation` returning `[]` for "40% faster across 12000 sessions" because the second figure was zipped against nothing — an unsubstantiated number leaving through the check built to catch unsubstantiated numbers; and a replay reporting `divergence at tick None`. Plus 15 silent swallows, each now naming what it lost, and `AVS_DEBUG=1` to give them a reader — 15 handlers logging to a logger with no handler would be ADR-060's defect wearing a different hat | ADR-055's `select = ["F"]` and the test that pinned it. The scope argument survives; what it loses is the assumption that "about behaviour" meant only `F`. Its one deferral — `S607`, 152 partial-path subprocess sites against a CLAUDE.md rule that says absolute paths — was written down rather than hidden, priced by the founder, and closed by ADR-064 before this release shipped. That sequence is the ADR working: an ignore-with-a-reason is a decision someone can overrule; a bare ignore is a decision nobody knows was made |
| [063](063-the-bench-is-not-on-a-timer.md) | *Why endless batch running?* — nothing said when to stop. The $67.88, five-hour bench was scheduled like the two loops that cost nothing: seven days since the last dated file. Run 19 was about to be bought on that basis over a framework that had not necessarily changed. The trigger is now the reading's own `avs_version`, floored at 7 days (ten releases in a week are not ten runs) and capped at a 90-day drift backstop. The cap is the load-bearing half: "run it when the version changes" is one sentence away from a watchdog that reports all clear forever, which is the exact failure this module names in its own source, so a test asserts the backstop is a NUMBER and not infinity — the way it becomes "never" is not a code change, it is someone setting it to 3650 "for now". A missing `avs_version` counts as CHANGED. Strictly cheaper and never more expensive, checked over 120 days of fixture rather than asserted in prose. `due_because` carries WHY beside the command in the alert, because `DUE (9d)` reads as a timer and a timer is no longer what raises it | ADR-036's weekly bench cadence, in the trigger only. Found on the way, by looking rather than by hitting it: an ADR-061 aftershock where the scheduler line went BLANK for a run with a null probe rate — the worst reading the series can produce |
| [064](064-one-lookup-with-a-name-on-it.md) | ADR-062's deferral, taken up: *fix all*. 152 invocations handed the kernel a bare name and let `PATH` decide — 60 in `src/`, 92 in `tests/`, 44 of them `git`. This system runs `git` inside a workspace it just generated from model output, and runs `npm install` in that same workspace; the list-argv rule stops an ARGUMENT becoming a command, and nothing stopped a COMMAND becoming a different command. One module, two functions, because absence means two things: `git` missing is a broken environment (`resolve` raises), `k6` missing is an ordinary Tuesday (`find` returns None and the lane reports `skipped` with the script it would have run). The load-bearing decision is that `ExecutableNotFound` subclasses `FileNotFoundError` — exactly what `subprocess` raised before this module existed — so `forge._run`, `github._gh` and the lanes written years earlier keep degrading to a note instead of crashing: a resolver that turned a handled degradation into an unhandled one would have been a worse bug than the one it fixed. Deliberately uncached, and honest that `shutil.which` still searches `PATH` — the claim is one lookup with a name on it, not no `PATH`. Three distinctions the conversion had to make: a command DISPLAYED for a human stays bare (`launchctl bootstrap` in `avs cadence`, `netem_command`'s recorded `tc` line) while a command EXECUTED gets resolved; where a `shutil.which` guard already existed its answer is now what runs, instead of being thrown away so PATH could choose again three lines later; and `_run` resolves `argv[0]` itself so the "`gh` is not installed" message it prints does not become a machine-specific path | ADR-062's `S607` ignore, deleted rather than grandfathered. A test walks `src/` with `ast` and fails on any bare-name argv head, so the rule is held by the code's shape and not only by the config. Found on the way, by the suite: resolving before `subprocess.run` MOVES THE TEST SEAM — `test_forge.py` declares itself hermetic and intercepted `subprocess.run`, which is now one frame too late, so six of its tests stopped exercising dispatch on a machine without `glab` and took the not-installed branch instead. CI would have gone green: GitHub's runners ship `gh` but not `glab` |
| [065](065-a-green-run-that-published-nothing.md) | The tag push IS the publish (OIDC), so force-moving a tag starts a second run while the first is still uploading — and the repo had already removed the symptom that would show a lost race. `uv publish --check-url` was added so re-tagging would not go red, and re-tagging is exactly the case where red was correct: "already on PyPI" is decided by FILENAME, so the run correcting a release builds a different wheel under the same name, skips its own upload, and reports success. PyPI then serves the pre-fix build, the tag points at the fix, and the release is green — unrepairable, since a version can only be yanked, never replaced. Now the publish job asks the INDEX what it is serving and compares sha256 per file, so a skipped upload, a half-uploaded cancelled race and a correct release are three outcomes instead of one green check; the message names the only remedy that exists (bump the version), not the one people reach for (re-run). Deliberately independent of what `--check-url` does when hashes differ: the guarantee wanted is a property of the index, not of the uploader. Plus a `concurrency` group keyed on `github.ref` — the key IS the correctness argument, since a constant group would cancel an unrelated release mid-upload — and `scripts/retag.sh`, which mostly REFUSES: if PyPI already serves the version, moving the tag cannot change what anyone installs and only makes the tag a lie. Refusal before cancel before push, because cancelling a legitimate run for a move that was never allowed is its own defect | nothing; it closes advice that was only ever written in an operator's note ("cancel the in-flight run for the old ref") — remembered at the end of a release, by someone who has just found a defect in what they were about to ship |
| [066](066-a-slice-is-not-the-suite.md) | *Any other way to run bench 19?* — yes, and the obvious one was the trap. `--limit N` sliced the case list BEFORE counting it, so `cases_total` was what the run was handed, not what the suite has: `--limit 1` wrote `1 of 1` into the tracked ledger. Not partial (`measured == total`), not aborted (the environment was fine), not simulated (real cases, real provider, full price) — every guard passed it as a complete reading. And the suite holds a case that has built nothing in two consecutive runs, so a slice landing on it reads `build 0% over 1 of 1`, below floor, and `CONSECUTIVE_RUNS_TO_FIRE = 2`: two purchases of a bench nobody could afford to run in one sitting would fire the criterion whose only remedy is a human deciding whether to kill the project. Third instance of one shape after ADR-053 and ADR-056 — *the cheap substitute for an expensive measurement corrupts the ledger the measurement lives in*. Now the denominator travels: `--limit` bounds what the run PAYS FOR, never what the scoreboard COUNTS, and each unreached case gets an `error: not run: --limit N` row that keeps it out of every rate (ADR-035) while saying WHY it is unmeasured, which a bare name list cannot. A truncated run stays in `.mas/` and out of `benchmarks/results/`, and `_scan` refuses one that arrives anyway and names it WITH the next step, since its checkpoints are banked. `truncated` is read off the ROWS, not the flag — `--limit 6` over six cases is a complete reading, and refusing it was a live bug in this change's own first draft, caught by a control run against the deployed build | nothing reversed; it narrows what may enter the capability ledger, the same narrowing ADR-056 made for simulated providers, and it makes slicing the SUPPORTED way to buy an expensive bench on a constrained account |
| [067](067-a-test-that-could-not-have-failed.md) | ADR-066's control, run over the whole population instead of one change. Thirty-two test files in `tests/` are named by an ADR as its mechanism; each is a claim about a defect someone had already seen, and none had ever been asked whether it would actually catch it. The control needs no judgement per ADR: check out the parent of the commit that ADDED the file, run today's test file there, and read what passes. **Roll back the whole tree, not just `src/`** — the first pass swapped only the Python source and reported ADR-065's file as 7-of-7 green, because its subject is a workflow and a shell script that were still being read from the current checkout; under the honest control it fails, as it should. Forty-four tests passed against the tree that predates them, and most of that is correct: the deliberately narrow half of a guard (`..._is_still_...`) passes on both builds by construction and its control is a different mutation. Five were real, each named for behaviour it did not pin — an ordering test that only looked at `runs[-1]`, where the misplaced file went to the front; a null-rate test that passed pre-fix because `float(None)` threw and the `except` returned `""` by accident, when the defect was in the writer; a folding test whose `findings[0]` was HIGH anyway because the list is severity-ordered; and two that kept their OWN COPY of the artifact they guard — a message retyped in the test, and three remedy names living in a `# (1) HOIST` comment. That last pair is ADR-051's defect wearing a regression test's clothes: two copies of one fact, and the guard watching the one it owns. Both now read the shipping text. Eighteen files fail to COLLECT against their parent commit, so that control cannot read them — and rather than leave them unresolved the control was escalated: per hunk, then per statement, then per LINE over every artifact the change touched (not just `.py` — a Python-only parseability check silently skipped all 27 changed lines of `publish.yml` and reported ADR-065's tests as survivors of a control that never ran), then condition negation for the absence-shaped half, then one hand-picked mutation per test for the residue. Each rung exists because the one above it was blind, and eight of the ELEVEN defects found in the instruments produced an EMPTY MEASUREMENT — six empty controls, one empty baseline, and one run that never returned, because the harness scoring ADR-036 killed the direct child only and a product server the mutated tests booted held the stdout pipe past the timeout, which is verbatim the defect ADR-036 exists to fix — and an empty measurement reads exactly like a passing one, the defect this record is named after, committed seven more times in the writing of it, twice as the fix for an earlier defect on the same list (a rename shim that was a symlink, so the next `git checkout` wrote the real package straight through it). The tenth is the one no denominator caught: pytest reports a parametrised test as `test_x[web]`, every rung intersected that against bare `def` names read out of the source, and a unit made only of parametrised tests therefore came back `baseline: 0` — six minutes of mutants reporting nothing about five tests that pass in four seconds. Four more real holes fell out: a test whose docstring said "end to end" and whose body built `Plan(status="proposed", …)` and asserted `status == "proposed"`; a guard that sliced `run_case`'s source for its `if` condition and ran past it into the fourteen-line comment below, which restates the condition verbatim in backticks — so the assertion was satisfied by the prose while the code said `<=`; a durability test that hand-wrote `built: true` into a spec, hand-committed it, and then asserted a working-tree discard could not lose it — git's behaviour, true of any committed file on any build, with the bookkeeping fix it is named for never executing inside it; and an ADR-060 guard that could not fail for ANY state of the code, twice over, because the audit it asks counts a field name in a `.md` as read (and `060-*.md` spells all six) and counts a bare string literal in a scanned `.py` as read (and the test's own tuple of names is exactly that) — the test was supplying every reader it then asserted existed. Reading the survivors found a fifth: `test_the_registry_is_the_only_place_that_names_simulated_providers` checks where the NAME `SIMULATED_PROVIDERS` is spelled and was blind to the five sites that ask the same question with `== "mock"`, which is how a second definition actually arrives. Ten real, not five — and the ladder run to the bottom reads 382 tests, 382 killed, 0 survived, 0 unresolved: 346 mechanically, 36 by a mutation named for each. The residue that no deletion can reach is six named shapes (absence-over-source, presence of a token older than the commit, a guard on behaviour the change deliberately left alone, a subject that is not code, a guard whose calibration lives in `tests/`, and behaviour with a redundant second mechanism that needs a declared two-point mutant) | nothing; no `src/` change and no version bump, so v0.111.0 stays the build every banked run-19 checkpoint is keyed to |
| [068](068-the-half-of-the-cli-that-never-runs.md) | ADR-067's question asked of the code instead of the tests. Mutation over 51,386 lines does not scale; its **complement** does — a line the suite never executes cannot be pinned by any test in it, so it is a guaranteed survivor established for 100% of `src/` in ONE suite run instead of a rung of a ladder. First coverage measurement this repo has ever had: 19,419 statements, **2,315 never executed**, 6,048 branches with **703 partial**, 93 functions with zero executed statements — and `cli.py` at **929 of 1,856 statements dark, 46.2%**. ADR-055 declined a coverage gate on "several hundred legitimately-untested branches"; the real figure is an order of magnitude larger, which strengthens its reasoning and invalidates its number, and both halves are recorded rather than quietly corrected. The measurement also turns ADR-054's closing sentence — *nothing caught it because no test invoked the command* — from a sample of one into a population: **42 of 78 commands have never been entered by any test and 30 are not so much as named in one**, including all four human judgment gates (`brief-approve`, `plan-approve`, `spec-approve`, `scr-approve`) and both commands that act on the world (`automerge`, `deploy-execute`). Not as bad as it reads, and the distinction is ADR-054's own: `policy.py` runs at 94% and the ADR-031 refusal path is covered — it is the CLI wrapper that is unmeasured, which is exactly the layer the eleven-run defect lived in, ten lines below where `--help` stops looking. So all 42 were RUN, in an empty tmpdir with `HOME` redirected and every credential stripped: 37 refused cleanly, 5 are servers, **0 broken**. A sound negative — there is no second ADR-054 today, and nothing would notice the next one. Two detectors (runtime trace, static scan of test sources) with zero contradictions; the static one undercounts by exactly the six commands built as `[*argv]` in `test_use_case_matrix.py`. Triaging the 93 dead functions collapses them, and the collapse is the finding: 10 are `@_register` MCP stage tools that name-based reachability cannot see — **reachable by an agent, entered by no test**, unmeasured rather than dead; 1 refuted outright (`ToolBox._list_files` via `getattr(self, f"_{tool}")`); 6 genuinely dead, two of them load-bearing — `is_terminal`'s docstring describes a three-way partition and its body returns `verdict in ALL_VERDICTS`, `True` for every verdict there is, and `record_calibration` is the only writer anywhere of a calibration file that is committed and therefore CANNOT BE REGENERATED by any code path, only hand-edited. Paid down the same day, **30 → 24**: `test_the_gates_a_human_touches.py` drives all four judgment gates plus `automerge` and `deploy-execute` through `CliRunner`, and a coverage run over that file alone confirms ENTERED rather than merely named (23/32 and 24/32 on the two act-on-the-world commands — `--dry-run` returns before `forge.merge` and before `subprocess.run`, so the suite enters every line of the decision and none of the action). What that buys is not the policy rules, which `test_automation.py` already pinned, but the seven lines of WRAPPER around them: `plan_result.tasks` then `t.id`/`t.description`, `data['spec_slug']`, four nested keys pulled out of a final YAML and passed by keyword — none of it checked by a test of the callee, all of it silently broken by a return shape that moves | nothing; no `src/` change and no version bump, so v0.111.0 stays the build every banked run-19 checkpoint is keyed to |
| [069](069-the-wrapper-the-ratchet-could-not-see.md) | ADR-064 closed CLAUDE.md's oldest half-kept rule — never a bare name in `argv[0]` — at 152 sites and left two guards: ruff's `S607`, and a static ratchet over `src/`. Both match an `ast.Attribute` call named `run`/`Popen`/`check_output`/`check_call` whose first argument is a list literal with a bare head. That is `subprocess.run([...])` spelled out, and nothing else — so `_run(["git", "worktree", "add", ...], repo)` is an `ast.Name` call that neither one looks at, and inside the helper the head is a PARAMETER, which no linter can trace back to the literal. **They were blind in the same place, which is why they agreed.** Asking the question they meant to ask — find functions that forward a parameter of their own to `subprocess.*` as argv, then find the calls handing those a bare-name list — reports **35 sites, 6 files, 19 executables**: the population was 187, not 152. The site that says the most is `testing.py`'s `npm test`, because `executables.py` OPENS by naming that exact call as the reason it exists (*"it runs `npm install` in that same workspace"*) and ADR-064's own ratchet repeats it — it reaches subprocess through `_run_and_classify` then `_run`, two hops, invisible to both instruments written to catch it. The motivating example was the unconverted case. Exposure stated exactly: **PATH order, not `cwd`** — POSIX `execvp` does not search `.` — the same exposure as the 152 already converted, an unseen class rather than a lesser one. Twelve of the 35 had ALREADY COMPUTED THE ANSWER: every availability gate ran `shutil.which(X)` for CLAUDE.md's visible-`skipped` rule, discarded the absolute path, and let PATH decide again at exec, so the binary checked and the binary run were never guaranteed to be the same one. The fix is smaller than the finding — **the gate's answer becomes the executable**. `build.py` is the sharpest illustration: it already imported `resolve` in ADR-064 and used it for its direct calls while two `_run(["git", ...])` sat two hundred lines away, untouched and unreported. The ratchet ships with no ledger and no allowlist because the count is zero, and with three guards — it refuses to report from a moved or empty tree, it runs the wrapper pass **to a fixed point** (the single-pass draft said 32; the fixed point said 35, and the three it missed included the `npm` call), and it asserts that it STILL RECOGNISES ITS OWN WRAPPERS, because zero wrappers found means zero call sites checked means zero offenders reported, byte-identical to a clean tree. The instrument's own defect is ADR-064's finding again: keyed by function NAME alone, `tools/debt.py::_run` silently overwrote `testing.py::_run` (argv at #1 vs #0), so the scan checked every `_run(["git", ...], repo)` against index #1, found `repo`, and reported a clean sweep it had not performed — over the exact sites it was written to find. **A name is a weak key, inside the instrument built to enforce the ADR whose lesson that is**; pinned by a test with two same-named wrappers rather than fixed quietly. Control: 35 against the pre-fix tree from `git archive HEAD`, 0 after, and reverting the single `npm` site fails the ratchet naming that line | nothing; it widens ADR-064's rule to the shape ADR-064 could not see. Five test seams move from `<module>.shutil.which` to `executables.shutil.which` — one PATH lookup means one place to stub, the resolver's argument all along — and the T3 hang test gained a `which` stub, since `resolve("docker")` would otherwise have made a hermetic test depend on a live daemon: a regression introduced by the fix, caught by the suite, fixed in the same change |

## Format

Each record states: context, the decision, what it reverses and why that
reversal is defensible, what stays out, and the mechanism that keeps the
new capability bounded. A record without a *mechanism* section is an
opinion, not a decision.
