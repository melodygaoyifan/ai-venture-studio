<!-- Moved out of README.md 2026-07-28: the release record is preserved
verbatim here; README links to this file and CHANGELOG.md. -->

# Release history (v0.8 → current)

<details>
<summary><b>v0.8 → v0.30</b> — the four downstream MASes, the founder autopilot, the adoption track, the outer product loop, domain profiles, the Sweep role, and the two audit gap-closure passes (22 releases, expand for the record)</summary>

| | |
|---|---|
| v0.8 ✅ | all four downstream stage MASes (code review, test gate, deploy review, maintenance) |
| v0.9 ✅ | greenfield autopilot for non-technical founders (FDR → product) |
| v0.10 ✅ | founder experience complete + measured (Studio UI, product benchmark) |
| v0.11–v0.12 ✅ | traditional-industry adoption track + hardening |
| v0.13 ✅ | product-loop substrate: typed claim ledger, `claim_lint`, evidence snapshots, synthetic-persona scan, source standing, `user_data_taint` |
| v0.14 ✅ | safe-publish: the seven deterministic marketing backstops, channel profiles, Gate PL3 scoped approvals — a human presses every publish button, and it never spends money |
| v0.15 ✅ | evidence: analytics/feedback privacy boundary, metric vocabulary with baseline-resetting definitions, cohort reads with sufficiency teeth, attribution typed at the tool boundary — only holdouts ground causal claims |
| v0.16 ✅ | upstream P0–P2: opportunity sensing, market & viability, PRD with required kill criteria, machine-checked handoff into Discovery |
| v0.17 ✅ | experiments: hash-pinned pre-registration, two-stage FDR-controlled screening→validation, sequential peeking, guardrail vetoes, inconclusive-enters-nothing |
| v0.18 ✅ | closed loop: `evaluate_kill_criteria` (a fired criterion cannot close without a recorded human decision), append-only kill registry, hypothesis reconciliation, the five loop metrics |
| v0.19–v0.20 ✅ | the outer loop operable end-to-end: gate CLIs, then the four LLM stages as one-command runs; first real-provider smoke (three wiring bugs found by gates, fixed); 24 voter fixture-registration gates + `voter-gate` |
| M2–M7 ✅ | build screenshots (gated-visible), in-Studio correction loop with SCR-backed scope changes, 验收清单 walkthrough, telemetry + digest, 微信支付/登录/订阅 blocks, estimate hints + undo |
| v0.21 ✅ | platform editions + distribution (docs 24–25): `init --edition` with narrowing-only `edition_lint`, the three START-HERE doors + weekly founder review + procurement pack, `replay --demo` (a real audit trail, offline, no API key), `--from-bench` templates that are the benchmark fixtures, opt-in aggregate-only `telemetry`, the published [benchmark page](docs/benchmark.md), and ADR-U29: this README is checked against [claims/platform.yaml](claims/platform.yaml) by the test suite — asserting beyond the ledger fails the build |
| v0.22 ✅ | the launch pass, prepared (P20): [launch/](launch/) holds the platform's own PRD (kill criterion: 4 weeks over the attention budget cuts scope at Gate PL5), a launch post that survives all its own backstops, and a hash-pinned pre-registered experiment whose power check honestly returns BLOCKED at current traffic — the suite keeps all three green; plus CONTRIBUTING (gated contributions, bus-factor-1 honesty, watch items with falsifiers) |
| v0.23 ✅ | complex-systems gap closure, deterministic core (docs 26–28, P21–P26): lintable perf ACs + VALID/INVALID load-run typing + `capacity.yaml` headroom arithmetic (perf lane calibrated: see roadmap v0.24); realtime delta (`det_sim_scan`, replay-identity/cross-build/desync checks, tick budgets); streaming delta (`stream_contract_check` — the word "default" is lexically illegal, exactly-once typed-or-downgraded, backpressure scan); architecture fitness (`deps.yaml` graph → `arch_contract_check`, brownfield checkpoint debt); delivery hardening (environment promotion DAG, `flag_lint` with owners+expiry, `migration_rehearsal` with reversibility round-trip) |
| v0.24 ✅ | lane execution wrappers + calibration: k6 scripts compiled from perf ACs (thresholds ARE the AC; absent binary = visible skip with the script on record), netem condition profiles with gated apply, schema-registry wrapper + contract-file reconciliation — and the seeded perf-defect calibration ran for real: 5 of 5 defects caught at the 3x relative-detection factor (loopback, low parity, honestly scoped — satisfies no AC), converting the lane from PROVISIONAL to CALIBRATED |
| v0.25 ✅ | the Sweep role (doc 29): `avs sweep` harvests the maintenance queues the ledgers already keep (expired flags, checkpoint debt, stale claims/capacity, due watch items, contract drift), patches only allowlisted chores under the behavior-preservation contract (out-of-scope diffs abort), caps open PRs to the attention budget, records clean passes with a snapshot hash, and alarms on over-action — SW0 report-only by default, promotion is your recorded decision at the weekly review |
| v0.26 ✅ | full design-doc cross-reference (docs 08–29 audited against the code): the [implementation map](docs/implementation-map.md) records every doc's shipped mechanism, tag, and named open items; gap-closure code from the audit — `operations-policy.yaml` (per-stage WIP + the shed rule + ci_concurrency), the `hot-files.yaml` shared-file registry + `lane_check`, the four 小程序 preflights (`mp_size_check`/`mp_domain_check`/`mp_setdata_lint`/`mp_privacy_check`), and `profiles/game.yaml` |
| v0.27 ✅ | audit gap closures, round two: GitHub Actions CI (the suite — including the self-linting README — now runs on every push, making "fails CI" literal), the §41.1 structured profile schema (add-only, composable), doc-16's cascade policy with the model-family heterogeneity floor + serial merge-queue admission (sweep never starves features) + the `gepa.yaml` budget schema (proposer-off by default), and the debt tools (radon/jscpd/vulture, availability-gated, feeding Sweep's queues) |
| v0.28 ✅ | gap-closure plan + phase A ([the plan](docs/gap-closure-plan.md) is a committed artifact; phases B–D queued): web det-tool runners (axe/Lighthouse/size-limit, gated), the data NFR grammar + lineage impact check, the full typed upstream verdict vocabulary, Gate P1 platform preflight (stale checklists and evidence-free checkboxes both fail; a named human submits), data-classification tags with downgrade refusal, and CHANGELOG.md |
| v0.29 ✅ | plan phase B: five profile voter charters (web DesignFidelity/A11ySemantics/PerformanceDelta, 小程序 PlatformFit, app DeviceReality) and 8-fixture registration gates for them AND the three data voters — `voter-gate web\|miniprogram\|app\|data` now serves every family under the same 87.5% contract |
| v0.30 ✅ | plan phase C: the cost/observability ledger (prices are config never constants, unpriced calls counted never zeroed, the monthly cap warns and a human decides; per-review tool-audit + evidence-ledger receipts; `/metrics` in Prometheus text, aggregate-only), the module-spec invariant layer (`.mas/specs/*.spec.yaml` — `SPEC_DRIFT_UNDOCUMENTED` on unexpected change patterns, forbidden side effects scanned in diffs), and named signal webhooks (`/webhooks/sentry\|datadog\|pagerduty` → the incident inbox, bearer-authed, deduped) |

</details>

| | |
|---|---|
| v0.31 ✅ | plan phase D, first half: the GEPA proposer loop (budget-gated by `gepa.yaml`, holdout-scored through the same fixture gate voters register through, proposal records only — nothing self-installs) and the secrets layer (`secret://ENV` loud resolution, masked repr, outbound scrub) |
| v0.32 ✅ | plan phase D13: the discover/plan/spec critics as fourteen registered charter voters on the shared stage engine (each behind its 8-fixture gate; failed gates exclude the voter, unregistered voters are reported), retiring the single-panel critique prompts — `voter-gate discovery\|planning\|spec` |
| v0.33 ✅ | plan phase D15 + D16 remainder: deploy review and maintenance rebuilt as checkpointed graphs on the shared saver — a crash mid-vote resumes from the last completed super-step via `recover` instead of re-paying the pipeline — and checkpoint rows encrypted at rest under `AUTOPRODUCT_CHECKPOINT_KEY` (honored or loud error, never silent plaintext; mirrors stay readable on purpose) |
| v0.34 ✅ | Studio live progress + the wire-up gate: the building page shows per-task state updating in place (signals s1/s3 — no more staring at a frozen page), interrupted builds surface per-module 继续 buttons instead of a dead confirm screen, and a bidirectional frontend↔backend wire-up test — every rendered button/link/fetch must resolve to a route, every route must be rendered by some state |
| v0.35.0 ✅ | the last small open items: registration gates for the six review voters (8 fixtures each, run through the real seat; the vote node fails closed and refuses to review with no roster), per-project `policy:` thresholds in `project.yaml` (unknown keys are a loud error, ranges bounded, a weakened bar stamped into the verdict), and the ready-queue fix — `next_tasks` read a field that never existed, so the plan never advanced past task one |
| v0.36.0 ✅ | the instrument for the v3.0.0 design gate: `avs loop` reads the cycle's own artifacts and reports the three criteria — and refuses to call the gate met on a quiet cycle or a recorded 'continue', because the gate is about the loop's ability to *stop* ([runbook](docs/v3-live-loop.md)) |
| v0.37.0 ✅ | MCP becomes the real internal tool transport (doc 11 §17): two subprocess-isolated partitions (`read_only`, `code_intel`) speaking JSON-RPC over stdio, the spec→host→server triple check where an unlisted tool is *unreachable* rather than refused, and the `mcp-audit` ledger recording every call with digested arguments — opt in with `AUTOPRODUCT_TOOL_TRANSPORT=mcp` |
| v0.38.0 ✅ | multi-tenant server mode ([ADR-030](docs/adr/030-multi-tenant-server.md), a recorded reversal of a non-goal — the server half only, never SaaS): token→workspace resolution where workspaces must be provably disjoint, per-tenant `secret://` webhook secrets, tenant-scoped reads, and uniform 401s that never enumerate tenants; plus [docs/adr/](docs/adr/) recording every reversal |
| v0.39.0 ✅ | policy-armed automation ([ADR-031](docs/adr/031-policy-armed-automation.md), the hardest reversal): `automerge` and `deploy-execute` exist but stay disarmed until a human writes an attributed, expiring policy naming exact branches — earned by track record, blocked on sensitive paths including the policy files themselves, and logging refusals as carefully as actions |
| v0.40.0 ✅ | the L1/L2 MCP partitions (deploy probes, maintenance correlation, and test execution — five subprocess servers now) with risk-tier RBAC: a partition above the caller's declared ceiling is never mounted, and the audit ledger finally covers the tools that touch the most; plus the deploy-branch fix, where an unresolvable branch is a refusal rather than an assumed `main` |
| v0.41.0 ✅ | the Context Manifest (content-hashed context assembly, grounding receipts where an unread contract is a violation, drift detection that refuses to build a spec edited outside the SCR channel, and overflow routed to Planning as a split) and research-session taint isolation (ADR-U03: consume research and the run loses L1+ tools for good, enforced at the transport rather than in a prompt) |
| v0.42.0 ✅ | grounding enforced on every build: the manifest is assembled, recorded, and checked against the prompt, so a required contract that never reached the writer blocks the build — which immediately found that module-spec invariants were absent from the implementer's prompt while Code Review enforced them, and they are now quoted verbatim |
| v0.43.0 ✅ | the first external-service tool (`sentry_get_issue` in the L1 maintenance partition): credential via `secret://`, a visible skip when unset, read-only by construction, and its payload wrapped as untrusted research so a hostile issue title is data rather than an instruction — wired into incident intake, and honest that it has not yet run against a live org |
| v0.44.0 ✅ | all six §17.2 signal readers (sentry, datadog, pagerduty, prometheus, loki, jaeger) over one shared read-only core, with two honestly-distinguished gating families — credential for hosted services, base URL for self-hosted ones, and a visible skip naming the variable either way |
| v0.45.0 ✅ | the deploy-side CLI wrappers (terraform/helm/kubectl/argocd/flagger/railway) complete §17.2's tool table: binaries gated on installation rather than credentials, read-only by construction (no sync/rollback/apply outside `--dry-run`, asserted on the source), and `kubectl_dry_run` client-side by default so a review never silently reaches into whatever cluster is current |
| v0.46.0 ✅ | the attention collector: `avs attention` derives the observable floor from gate dwell and recorded decisions, refuses to author the number itself, keeps the series append-only, and exits 3 demanding a human decision the moment four consecutive logged weeks breach the budget — the v3.0.0 blocker was an unautomated habit, not a missing four weeks |
| v0.47.0 ✅ | the bot fleet (doc 17 §45.2's last unbuilt check): defined by a session protocol rather than by a game, so its detectors are verified against real sessions of a real deterministic sim — and the first real run immediately found a dedupe bug that a stubbed stream would have hidden |
| v0.48.0 ✅ | upstream resume (a crashed `create` skips tasks already built instead of re-paying them — task-granular, and said so plainly), grounding extended to the spec writer (which immediately showed the spec writer never saw CLAUDE.md or the module invariants it must not contradict), and the gap-closure plan closed out |
| v0.49.0 ✅ | the use-case matrix: every profile builds, every edition narrows, every substrate rung activates exactly what its floors allow — which found that six of eight stages knew their infrastructure floor without enforcing it (an S0 team with no git could still run `build`), now fixed and pinned in both directions |
| v0.50.0 ✅ | `loop` and `attention` joined: the gate report now names the real distance to firing and the exact command to run next, and reports a `not_tracked` week as the recorded decision it is rather than as logged or as a gap |
| v0.51.0 ✅ | a second kill-criterion axis, chosen by the human and evaluated by the machine: product-bench capability regression, whose series is already collected weekly — so unlike the attention axis it can fire on the next run, with floors read off the observed distribution rather than picked |
| v0.52.0 ✅ | the Studio speaks English (`--lang en`): every string in a per-language table, an English FDR template, and the README's founder demo replaced with an English-first walkthrough plus a real screenshot of the real UI — the demo previously claimed English while showing a Chinese interface |
| v0.53.0 ✅ | renamed to **ai-venture-studio** (live links updated; recorded evidence locators left as recorded, with a rename note, because a snapshot is not edited after the fact) and English became the Studio default — `--lang zh` restores the original bilingual UI character for character |
| v0.54.0 ✅ | package + CLI renamed to match: `import ai_venture_studio`, command **`avs`** (with `autoproduct` kept as an alias so nothing scripted breaks) — while env vars, Prometheus series, telemetry keys and the CLAUDE.md section header were deliberately left alone, because renaming a wire format silently breaks its consumers |
| v0.55.0 ✅ | Studio modes (`--mode founder\|engineer\|enterprise`): the mode resolves from the workspace edition so different users get different depths of the same UI — founder unchanged and default, engineer adds task IDs/states and the CLI equivalent of every button, enterprise adds the resolved edition's governance facts and the attestation-ledger count. Modes only ever ADD visibility (tested structurally: every founder form survives in every mode) |
| v0.56.0 ✅ | per-mode UIs, researched then built: the mode is now adaptable per request (visible switcher on every page, `?mode=` + cookie, never auto-flipped — Findlater & McGrenere CHI 2004), engineer mode gains the review timeline in the browser (`/review/<id>`, the mirror `avs replay` reads) and a voter-health board, enterprise mode gains attestation *chain verification* (tampering renders as BROKEN-at-entry-N, not a line count), the S0–S4 stage-activation grid, the F-18.3 dwell/rubber-stamp report, and automation arming state — every card a pure read of files the CLI already writes |
| v0.57.0–v0.57.1 ✅ | the six gaps from the research/comprehension audit (`init` stops overwriting CLAUDE.md, the Context Manifest actually reaches the writer, `avs map` / `init --adopt` derive a brownfield codebase map from the code, charter voters get their declared tools, `--tier` reaches the planner, `avs probe` is the quarantined fetch the docstrings had promised) — and then the Studio stopped ever showing "Internal Server Error": the long LLM handlers gained the in-flight guard and the plain-language failure page the build path already had |
| v0.58.0 ✅ | the MVP contract (`avs mvp`), which doc 13 specified and nothing had implemented: the system could make a build smaller but never asked whether the slice, on its own, settles a hypothesis — plus the AI delta fired by AI-shaped language in the founder's own words (a named simpler alternative, a declared cost of being wrong, a wrong-answer fallback, ≥20 eval cases authored first, a quality metric paired to every volume metric), and `scope_tier` finally carried from Gate PL2 into the planner |
| v0.59.0–v0.59.1 ✅ | the cost gate: spend is measured per run and the cap is real at Gate 1 — then corrected, because transparency is the point and the cap is the opt-in secondary |
| v0.60.0 ✅ | a run that says what it is doing, and a failure that says why: per-module narration in terminal and Studio, no percentages and no ETA, and a truncated model response named as such instead of surfacing an iteration later as an unrelated test error |
| v0.61.0 ✅ | the enterprise you actually work at: GitLab as a first-class forge behind a `forge.py` seam, Bedrock and proxy paths to the model APIs, the Studio governance dashboard and production loop, air-gapped evaluation, and the ready-to-build preflight |
| v0.62.0 ✅ | the run that could not build, and the page that never said why: `avs create` could not build a single task on any FDR — the implementer's 32k `max_tokens` made the SDK refuse the request before sending — plus a 529 retry budget that expired in fourteen seconds, a good brief thrown away by a botched final revision, EARS rejecting correct requirements, a report that blamed the founder for our own blocked specs, and a conversational way in that is now the front door |
| v0.63.0 ✅ | a 小程序 that opens, and a repair that has to prove itself: the loadability gate and the scaffold behind it (nine modules built, zero reachable, because nothing checked for `app.json`), the fix iteration that can now be vetoed by its own re-review, the JS glob that ran preserved failed attempts as if they were the product, grounding that no longer mistakes a reformatted artifact for a missing one, resume that can name the work it skips, and EARS's optional `then` |
| v0.64.0 ✅ | the rules that were only ever sentences: Gate U2's scope lock reused instead of re-decided (keyed on an FDR fingerprint; a changed FDR after the lock is refused with the SCR/`--replan` routes named, while the Studio's own requirements form releases it, because rewriting the FDR there IS the deliberate re-decision), `retry-task` running the same Gate 3 and carrying the same contract as `create` (four of seven modules in one real run had reached the founder unreviewed), per-profile language enforced at the spec AND the write boundary — and the mock finally answering in the language the profile runs, since a Python item-store for a 小程序 meant 1478 green tests over a product WeChat could never load; plus `avs mp-runtime` (real DevTools, every registered page, local-only, a disabled service port reported as skipped rather than failed) and **`avs smoke`, now step 0 of every release**: four real calls per provider against the boundaries a mock cannot model, which on its first run found a reasoning model's null `content` reaching every caller's `.strip()` |
| v0.65.0 ✅ | real auto development, two halves: **the run presses its own retry button** (one bounded pass after the first, dependency order, cost-cap-gated, recorded in auto-approvals — mechanical failures only, judgment gates untouched) with the previous attempt's diagnosis carried into both the spec writer's and the implementer's prompts so a retry is a different attempt rather than a replay, plus the ghost-row fix that had a fully-built product reporting `failed`; and **the cost cap can fire** — `avs prices --import --cap <usd>` ships published list prices with a vendor URL and retrieval date per entry, ranges resolved upward so the estimate is a ceiling, your corrections surviving every re-import, and an unsourced model staying visibly unpriced rather than invented |
| v0.66.0 ✅ | the spend guard, built from research on non-technical builders (whose top recorded fear is the surprise bill, on platforms that set no cap by default): the Studio cost card becomes **Spending & cap** — the monthly ceiling set in one click into the CLI-owned cost-model, shown on the confirm page next to the button that starts the spend and before the first dollar, "paused not broken" when the cap fires, floor-labelled when a call is unpriced; mode-adaptable add-only (engineer gets the per-model table + CLI twins, enterprise the governance note); plus `VERIFICATION.md` — the founder's own requirements probed against the built product — finally linked and served instead of written and invisible |
| v0.67.0 ✅ | spend is measured and reported, never gated ([ADR-032](adr/032-no-framework-spending-cap.md), operator decision): the v0.65–v0.66 monthly cap removed everywhere (Gate 1, per-task build, auto-retry precheck, Studio ceiling form, `--cap`) because every call is billed to the operator's own key or subscription and budget enforcement belongs to the provider that does the billing — while every visibility surface stays (report cost line, `avs cost`, the Studio card before the first dollar, the sourced price table as estimation, FLOOR labelling of unpriced calls) and the removal is pinned by tests as firmly as the presence was |
| v0.68.0 ✅ | a blank page is no longer a passing page: the 小程序 runtime check reported **all 7 pages rendered** for a build where three were pure white — `reLaunch` not throwing was its only signal, and it does not throw for a page whose JS died before `Page()`. Now the static gate walks every relative require chain (unresolvable, or escaping `miniprogramRoot`, means a module that throws at evaluation and a page that renders blank) and `avs mp-runtime` screenshots every page and **fails a single flat colour as `page_blank`** — cause-agnostic, stdlib-only PNG decode, validated in both directions against the incident; plus the driver defects behind it (the automator client hangs on IDE 2.01.x, so the protocol is spoken raw; a reused automation port once verified the wrong project, so each run spawns and tears down its own). With it: a re-run's first attempt at a previously-failed task now carries the previous run's diagnosis (it was blind across runs), the Studio continues an interrupted build in one click instead of one per module, and the scaffold's index page no longer ships blank by construction |
| v0.68.1 ✅ | the diagnosis stops lying, and the Studio reads like a product: the 小程序 automation timeout was wrong three ways — blaming a service port that was already on, then reading a `✔ auto` from an earlier run out of an append-only log as *this* run's success (so a session that exited in 4s was reported as 300s of first-open cost) — now scoped to what this run wrote, with the wait measured and an early exit as its own diagnosis; a stray directory named `miniprogram/` no longer buys the loadability gate's silence over a root-layout project (evidence beats directory names); the scaffold's `.gitignore` stops a future AppID leak from DevTools' per-machine files; the guide records that `libVersion: "latest"` is FINE (the pin was an automator workaround) and that the IDE degrades over a long automation session. Plus the Studio restyled into a product — one white card, one green primary action, a stage rail, rendered CONFIRMATION/BUILD-REPORT with the will-NOT-build callout, elapsed clock and accrued spend that were already on disk, AA-legible hints — every route unchanged, add-only intact; and a task id from a form is never taken on trust |
| v0.69.0 ✅ | the key, the paragraph, the product, and the decision — the four proposals the Studio redesign left unbuilt: a **key gate** that meets a founder in whose-account-pays language instead of a stack trace (the doors that need no key typed here, `/demo` as the no-key rung inside the browser, a pasted key held by that process only and never written to disk — and **not offered at all on a token-gated Studio**, where the process is everyone's and one person's key would pay for every build anybody on that token starts); **one open paragraph then only the gaps**, where the charter rule is enforced in Python rather than in the prompt (a "said" value that is not a whitespace-normalised span of the founder's own words is demoted to a guess, and a guess never reaches FDR.md unconfirmed — verified against a deliberately lying model); **`/try`**, the acceptance criteria beside the product as rows whose complaint travels with the row, where a tick is the founder's note and never a verdict, and the Studio refuses to spawn a server nobody owns on a port nobody chose; and **the decision before the work** — a complaint classified as small fix or new requirement before it costs anything, plus the change log as the undo surface, each entry offering *go back to just before this change* and stating how many later changes go with it, because the history is a straight line and pretending otherwise would promise an independence the model does not have. Plus the wire-up gate's missing half: it rendered pages and never PRESSED anything, so two unvalidated path segments lived in that gap (a `slug` that walked the build out of the workspace, a `task_id` that spawned a worker doomed on arrival and left its pid file behind) |
| v0.70.0 ✅ | what a real run said, and the repair for what it broke: one live English run through the shipped Studio (driven in a browser to replace the README's pre-redesign screenshots) found six defects a green hermetic suite had not — **every artifact a founder reads now follows the FDR's own language** (the confirmation prompt *demonstrated* `会做什么 / What will be built` while instructing "same language as the FDR", and a demonstration beats an instruction, so the page a founder reads before spending money came back in Chinese over an English FDR; headings, tally and cost heading now keyed by a deterministic CJK check); **`built: true` rides inside the task's own commit** (written afterwards it was uncommitted, and a rolled-back fix iteration or a failed-build workspace reset discarded it — the wrong headline "2 of 6" was the visible half, the expensive half being that `built_task_ids` is what a resumed run reads to decide what it may skip, so four committed modules would have been rebuilt and re-billed); **`avs reconcile [--scan] [--apply]`** repairs workspaces that already carry that damage, on two-artifact agreement only, keyed on TASKS because keyed on spec files it called nine of eleven findings falsely — a re-plan's superseded specs are not lost flags; **the wait stopped claiming "$0.00"** through 24 minutes of real spending (the guard covered no-calls but not unpriced calls, and the floor marker was stripped off the figure); and **one NOW instead of three** on a sequential build. The README's founder demo is that run, and its honesty test moved with it rather than being relaxed |
| v0.70.1 ✅ | a name declared twice renders every importing page blank: the 小程序 loadability gate now reports duplicate top-level function/const/let/class declarations per module — found when a second `pad2` in a utils file left the cart, delivery and profile pages registering no `Page()` while **106 node --test cases stayed green**, because Node loads the file as a script and the 小程序 toolchain compiles it with ES-MODULE semantics where a duplicate is a SyntaxError (verified — it is legal even in strict mode, so the obvious explanation was wrong); `var` excluded, since re-declaring a var is legal everywhere. Third member of the same family after the missing page file and the require that escapes miniprogramRoot. Also the README media assertions follow the v0.70.0 screenshot rename. First release whose publish run enforces PC-1 itself |
| v0.71.0 ✅ | the recurring loops get a trigger, and the build loop gets a floor: three loops designed to recur (§09.8 compounding, doc 29 Sweep, doc 25 §76.4 attention) had no trigger, and this repo is the evidence — `metrics/attention-log.yaml` records the discipline starting 2026-W31 and then stops, with nothing watching. **`avs cadence`** reads the artifacts the loops already write and says which is overdue, exiting 3 so it can gate a script; a loop that never ran reports `never_run` rather than fresh, a `not_tracked` week is not a run, and a future-dated artifact clamps to zero. The trigger is a machine-local LaunchAgent and had to be — every artifact these loops read lives under gitignored `.mas/`, so a CI runner would find every loop `never_run` and get tuned until it reported a clean pass forever against state it cannot see. Written disarmed (`--install` writes, `--arm` loads, `RunAtLoad` false), and `avs attention` only ever runs read-only: mechanical recurrence is the machine's job, the judgment is not. Plus a **termination bound** on the build loop (`--token-ceiling`, default 10M, status `halted` not `failed`) — tokens never dollars, ADR-032 stands |
| v0.71.1 ✅ | the trigger gets an environment: **launchd does not read a login shell**, so a credential kept in `.zshrc` is simply absent at 09:00 and `compound` would have failed every morning into a log nobody opens — the silent-success failure the feature exists to prevent, reintroduced by its own installer. The plist now carries pointers (`*_KEY_FILE`, `ANTHROPIC_BASE_URL`, `AWS_PROFILE`) and **refuses secrets by name**, because the plist is a readable file in `~/Library/LaunchAgents` and writing a key into it would turn the scheduler into a credential leak; PATH is set explicitly; no credential at all is a warning at install time rather than a discovery a week later |
| v0.72.0 ✅ | a loop that read nothing no longer reports as a loop that worked: `cadence` decided freshness from the *existence* of a dated artifact, so `avs compound` over an empty 7-day window wrote a proposal without calling a provider — and that file read as a healthy run for a week. Read-nothing is now `vacuous` (`ok, empty`, named in the summary) and distinguished from read-plenty-and-concluded-nothing, which is a real negative result; Sweep is exempt because invariant 14.30 makes a clean pass a finding. Also: **`__version__` had drifted to 0.70.1** and shipped that way in two releases because nothing read it — now pinned against `pyproject.toml` by a test |
| v0.72.1 ✅ | the compounding loop writes down what it spends: found by arming the scheduler and reading the ledger — `avs compound` called a provider, produced a real proposal, and `.mas/spend.jsonl` showed **zero calls**. The adapter buffers usage in process state and only a caller that knows the workspace can flush it; every other provider-calling path did, this one never had. Survivable while hand-run; not survivable on a daily LaunchAgent, where it becomes a standing recurring unmetered cost |
| v0.72.2 ✅ | metering stops being something each command has to remember: fixing the one caller that was caught is how the same bug comes back, and the audit that followed found **2 of 77 commands** flushed. Now every command flushes once, in one wrapper, to the workspace it was already pointed at — inert for commands that never call a provider, and an unwritable ledger can never mask the command's real result. ADR-032 removed the spending *cap* and kept the metering; this makes the kept half structural rather than a rule each new author has to already know |
| v0.72.3 ✅ | the scheduler tells you when it is running an old build: releasing v0.72.2 exposed the gap between *published* and *deployed* — the plist names an absolute path to whichever `avs` was on PATH when the agent was armed, so `git push`, a green publish and a new PyPI version move nothing on the machine, and the daily loop went on running v0.72.1 including the metering fix it did not have. `avs cadence` now asks the scheduled binary's own interpreter for its **distribution metadata** (what pip wrote, which cannot drift — `__version__` is hand-maintained and has), prints the upgrade line, and **exits 3**, because a yellow line gets scrolled past and the exit code is what a script reads. Also `avs --version`, which did not exist |
| v0.73.0 ✅ | a founder says three things and gets three answers: one message listing three problems produced one classification card, one repair, and a return to the home page — the other two were not refused, not queued, not logged, and nothing on screen said so. Self-concealing, because reporting a problem twice is what a founder does when the first report seems to have been read. It was singular in three places at once (`_ROUTER_SYSTEM` asked for the ONE responsible feature and gave the model no field a second issue could occupy; nothing compared the routed issue against the message it came from; the repair stage collected sources for one spec). The router now splits first and returns a **list**, each route carrying the founder's own words for *its* issue — checked as a whitespace-normalised span the way intake checks a "said" value (§13.26.7), because a summary shown back under "Your words" is a lie the founder cannot catch |
| v0.74.0 ✅ | "actually, make it X" is a button now, not a sentence: a requirement change ended at `re-run \`avs add\` for 'cart' to regenerate` — a terminal instruction printed to someone in a browser, under a green tick claiming it had been handled — while the SCR was raised and approved at *classification* time, so a founder who read the plan and walked away left a banked free pass to edit a frozen spec. The change now comes back as a **draft** (what changes, and the assumptions made on their behalf, stated flat so a wrong one is visible before it is built), and pressing it is what authorizes: SCR in the founder's own words, then the amendment **parked** as `.mas/pending-amendment.yaml` — because the Studio confirms in one process and builds in a detached one minutes later — bound to its FDR by digest so an abandoned record cannot attach to an unrelated build, and spent by `run_feature` only when every task built, since a spec promising behaviour that failed to build is worse than the stale spec this path exists to fix. Amending **re-stamps `approved_hash`**, because `criteria` is inside `contract_hash` and re-stamping is what ratification means — without it the founder's own approved change would look like an unratified fork and §13.35.5 would refuse every later build. Plus the other half nobody would have noticed until the first one worked: **the evidence is refreshed after a change** (screenshots and `VERIFICATION.md` came only from the first build, so the founder would have changed the cart and been shown the old cart under a green success message), screenshots now discover the product's own routes instead of photographing the front door eight times, capture takes a free port instead of a hardcoded one it now revisits, and a build that could not be photographed says why on the page instead of rendering an empty space |
| v0.75.0 ✅ | something to point at, instead of something to remember: everything a founder had to do *before* "actually, make it X" was recall. The home page's `Features` section listed `product/features/*` — the directories written by post-build *changes* — as bare slugs, so a founder who built six features and changed none saw an empty section describing a six-feature product, and then had to describe from memory a feature the Studio had never named for them. There is now a **card per built feature**, rendered from `built_specs` (the correction router's own list, deliberately — two readers of `specs/` would eventually disagree, and the visible failure would be a card offering a change the router refuses to route), each showing what it does as a **sentence rather than as EARS** (`Keep the cart after the browser is closed.`; display only, since `criteria` is inside `contract_hash` and rewriting it would be an unratified spec edit dressed as a stylesheet) and carrying a `Change this` box. Pressing it sends `spec_slug` and **constrains the router to that feature without skipping it** — which feature is a fact the founder pointed at; broken-vs-requirement-moved is a judgment they did not express by pressing a button, and deciding it here would answer the one question nobody asked. Marking an acceptance row wrong **needs no typing**: one tap sends *this one is not right: <the row>* (a report of a failure, not the bare row text, which the SCR would file as a requirement), with the box still there, second, for anyone who wants to add words; a row that is a criterion word for word carries its slug, a row that matches nothing carries none — `criterion_owners` returns only exact, single-owner matches, because a *wrong* pre-scope skips the router precisely when the router was needed. And the composer's three tabs became two: `Something wrong?` and `Is it broken?` posted to the same form and the router reads the words, never the tab |
| v0.76.0 ✅ | what happened, said to the founder, and takeable back: three failures on one screen — the card a founder reaches after asking for a change. **It was written for the wrong reader** (`repaired in 2 attempt(s); files: src/cart.py` was the whole message, so the person this product exists for was handed the implementer's notes and left to work out the two things they wanted: did my product change, and what do I do now). A sentence written for a log cannot be translated afterwards, so `CorrectionResult` now carries a `reason` from a closed vocabulary — one member per way out of `run_correction` — with a test asserting every member has a string in **both languages** and that every outcome which changed nothing says so out loud; `detail` moves one fold down rather than being deleted, because it is exactly what a founder's technical friend needs. **A repair could not be undone**: builds and feature additions were checkpointed, repairs were bare commits, so the one kind of change made when something is *already* wrong was the only one the undo list could not see — repairs now tag a checkpoint, and the offer to go back sits beside the thing it reverses, carrying the same honest note about how many later changes go with it (one renderer for both, so the cost cannot go missing from one). **And the page you wait on looked exactly like a hung one** — reloading every four seconds through the minutes a model call takes, saying the identical thing each time; it now shows how long it has been running, a fact rather than a progress bar |
| v0.77.0 ✅ | "not that", said with one tap: a requirement change came back as a draft listing the **assumptions** the model had to make because the founder did not say — put there deliberately, so a wrong one was visible before it was built — and then the only control under them built all of them anyway. Seeing that an assumption is wrong and being able to say so are different things: the founder's entire vocabulary was one button meaning yes and a browser back button meaning "start the complaint over from nothing", which for the reader this product is written for is a dead end with a list of reasons in it. Every assumption now carries **Not that**: one tap rejects that decision and the draft is written again, with the rejection sent to the model as a question it must answer **differently** (a redraft that keeps the same behaviour under a new sentence is a wrong answer, and the prompt says so). Words stay available and stay **second** — a tap is already a complete answer, and asking for a sentence is the toll this path exists to remove. The rejections travel on the plan in the hidden field it already used, because the draft is never written down and the plan is the only memory between requests; the complaint is **not re-routed** (a second router call could classify the same words as a repair, or against another feature, and the founder would be shown a redraft of a change they never asked for), so `ChangePlan` also carries the router's `instruction` and the rejection is the only thing that differs. Two rounds, the bound intake already puts on clarifying questions, enforced on the **route** as well as the page so a stale tab cannot spend a call the card stopped offering — and **Make this change** is on the card at every round including after the cap, because the limit ends the redrafting, never the change |
| v0.78.0 ✅ | an empty window says which kind of empty: `avs cadence` reported the compounding loop **green over nothing** — it had read 0 reviews for weeks at the workspace it watches, and the only sentence it had for that was `read 0 reviews — nothing to compound`, which names the symptom and no cause. The two causes are opposites: a loop pointed at a workspace nobody ever built is a misconfiguration to fix; a loop over a workspace where the work paused is a loop doing its job. A founder cannot tell them apart by looking, and this is the exact failure mode v0.72.0 shipped the `ok, empty` state to expose. `collect_signals` now counts **every** review on disk and keeps the newest `written_at` regardless of the window — the distance between that stamp and the window *is* the diagnosis; `render_proposal` writes it onto the artifact, `cadence` reads it back into `LoopStatus.empty_because` (`work_stopped` | `never_any` | `""`), and the CLI turns it into the line worth acting on (*almost certainly pointed at the wrong `--repo-dir`* vs *the loop is fine; the work is what paused*), with the age measured from the stamp and clamped at zero so a future date reads `0d old`. Also: `scr_raised` left `correction.py` four releases ago and stayed behind in the results page's colour table and the CLI's as a row nothing could reach — a dead row is not a visible bug, which is why it survived. Both presenters now key off a single `correction.STATUSES`, gated by tests read off the **source** rather than by exercising the paths, because the failure being guarded is a status no path reaches |
| v0.79.0 ✅ | the alert leaves the machine: the daily LaunchAgent had been noticing correctly since v0.72.0 and telling **nothing that listens** — it exits 3 when a loop needs a person and writes the reason to `~/Library/Logs/ai-venture-studio/loops.log`, a file nobody opens, so the machine knew and the week passed anyway. That is the silent-success shape the loops exist to catch, one level up, and "check the log weekly" is not maintenance — it is a reminder the operator has to set themselves. `avs cadence --notify` posts to a Discord webhook under three rules, each of them a way a notifier is worse than a log: **only when something needs a person** (no daily all-green, because a channel that speaks every morning gets muted along with the one message that mattered); **not the same thing every morning** (the alert is identified by a hash of its *content*, never its date, so tomorrow's identical alert is recognised as already said and held for 7 days — while a *changed* alert goes out immediately, since delaying new news would make the channel less trustworthy than the log it replaces); and **carry the command, not the diagnosis** (the reader is on a phone: every line says what is true and hands them something to paste, including the `avs attention` command marked as the one no scheduler can answer, and the v0.78.0 empty-window cause, which has to survive the trip). Setup is one command — `avs cadence --set-webhook <url>` validates before writing (an unvalidated URL sits there looking configured and fails once a week at 09:00, into the log this feature exists to stop using), stores it `0600`, and never echoes it back, so `resolve_webhook` finds it with **no environment at all**: launchd hands a job four variables and no login shell, and a feature configured only by an `export` would be missed by the one run that counts. The webhook URL is a credential — whoever holds it can post as this app — so `AVS_DISCORD_WEBHOOK_FILE` travels into the world-readable plist and `AVS_DISCORD_WEBHOOK` is refused there by name; the missing-credential check moved from `*_FILE` to `*_KEY_FILE` in the same change, because the webhook pointer also ends in `_FILE` and a workspace that could notify but not authenticate would otherwise have installed clean and failed every morning. A failed delivery is never recorded as sent (the dedupe would suppress tomorrow's retry of an alert that never arrived — silence, remembered as success), and `--notify` leaves the exit code alone: telling someone is not fixing it |
| v0.80.0 ✅ | the alert carries the error, not just the backlog: v0.79.0 sent Discord the *lateness* — which loops are overdue, which kept cadence over an empty window, whether the scheduler is behind — and not the thing most worth waking up for, a loop that **ran this morning and crashed**. `run_due` has captured every due loop's exit code and the last 2000 characters of its stderr since v0.72.0, printed them, and dropped them; `build_alert` was never given them. So a workspace whose `compound` loop died on a traceback at 09:00 produced a green table, no alert, and a stack trace in the log nobody opens — the exact failure v0.79.0 was written to end, one layer further in. `build_alert` now takes the run's `RunOutcome`s and separates **ran and failed** (`exit N`, plus the *tail* of the output in a code fence — the traceback's last line names the error, the first fifty are import frames) from **could not be started at all** (missing binary, bad `--repo-dir`, timeout), because those are different fixes. Three things are deliberately not errors: a loop that was **not due** (that is the schedule working), a run that **succeeded noisily**, and a loop that **needs a human** — `attention` exits non-zero every single morning by definition, and calling that a failure would have the channel cry wolf daily about the one loop behaving exactly as specified, when it is already reported correctly as overdue. Failures are written **first** in the body and take over the heading (`2 loops FAILED this run: sweep, compound`): `render` truncates from the end to fit Discord's 2000-character ceiling, so an error written after a long backlog is one that can be silently cut, and the heading is the phone's notification preview — the whole message for anyone who does not open it. Known limitation, stated rather than papered over: this reports failures the scheduler *survived*; if `avs cadence` itself dies before the notify block, the log is still the only record |
| v0.81.0 ✅ | the weekly attention axis is withdrawn, and the scheduler now asks nobody anything ([ADR-033](adr/033-withdraw-weekly-attention-axis.md)). The launch PRD's first kill criterion measured the framework's own weekly maintenance hours against a 4.0-hour budget over 4 consecutive weeks, and its only instrument was a number the operator had to type in every week. Three weeks after launch the log held one `not_tracked` row and zero logged hours — so the series measured willingness to answer a weekly prompt, not maintenance load. The care around it was real (`avs attention` refused to invent a number, logged `not_tracked` rather than estimate, and the watchdog could never answer on the operator's behalf) and did not help: an axis whose upkeep is a weekly typed number will not hold against a founder for whom typing is the most expensive thing the product can ask. It also cost something concrete. The `attention` loop was due every 7 days and exited **non-zero every single morning by design**, because "not yet answered" is its normal state — which forced a permanent exemption in the alert path shipped one release earlier: the one loop guaranteed to fail daily had to be excluded from the error channel or the channel would cry wolf every morning. A scheduled job whose non-zero exit means nothing is a hole in the error reporting, and it existed solely to carry this axis. Removed: `attention.py`, `metrics/attention-log.yaml`, `metrics/weekly_maintenance_attention.md`, the `avs attention` command, the `attention` cadence loop, `LoopStatus.human_input_required`, and the cycle report's attention axis. PRD outcome **O-L1** and its kill criterion are withdrawn from `launch/prd.yaml` with the withdrawal recorded in place and the id not reused; the capability axis (**O-L2**), whose series `benchmarks/results/*.yaml` is written by the weekly run itself, stands and can fire without asking anyone anything. The Gate PL5 record of 2026-07-26 is **not rewritten** — its reading stands verbatim with a `superseded_by` block appended, because the criterion was *withdrawn, not satisfied* — and the pre-registered launch experiment and the published launch post are left exactly as recorded for the same reason. With the human loop gone the exemption goes too: **a non-zero exit from any scheduled loop is now a failure, full stop**, and reaches Discord |
| v0.82.0 ✅ | the kill criterion's series becomes a watched loop, and its schedule leaves cron ([ADR-034](adr/034-the-bench-is-a-watched-loop.md)). v0.81.0 left the product-bench capability axis as the launch PRD's **only** kill criterion, on the strength that its series is collected mechanically and can fire without asking anyone anything. The series had stopped: newest result run 11, `result-2026-07-27-0449.yaml`, sixteen days and three scheduled Mondays earlier, with no complaint from anything. Two independent defects, either sufficient — the Monday crontab entry never fired (cron *skips* a job whose minute passed while the Mac was asleep rather than deferring it; the bench log holds exactly one entry ever, the preflight of the day it was installed), and it could not have authenticated if it had (the script grepped `^export ANTHROPIC_API_KEY=` out of `.zshrc`, which the v0.71.1 LaunchAgent hardening had since converted to the `ANTHROPIC_API_KEY_FILE` form). Neither is the structural fault: `avs cadence` watched `compound` and `sweep` and did **not** watch the one series a kill criterion reads, and a criterion whose series has silently stopped reports "not fired" forever — indistinguishable from a criterion being satisfied, which is the same absence-as-clean-pass failure ADR-033 removed, one directory over. So `bench` is a third cadence loop: seven-day cadence, read from the newest ISO date in `benchmarks/results/result-*.yaml`, carrying the build and probe rates that run recorded, and **tracked only where its cases live** — no `benchmarks/products-real/`, no bench loop, because the bench measures the framework rather than a product and a standing false alarm in every product workspace would train the reader to swipe the channel away. It runs itself (`avs product-bench --cases-dir benchmarks/products-real`, the cases directory named explicitly because the command's default is the synthetic set and the criterion is defined over the real one), so ADR-033's rule holds — a paid hour-long run is still a run, not a question. Per-loop timeouts arrive with it: run 11 took 74 minutes, and one shared hour would have killed it at the three-quarter mark and reported the timeout as a capability failure. `--only` and `--label` give a second workspace its own schedule, with `--only` validated against the loop names the module knows rather than what happens to be present and an absent loop an error rather than an empty report, because a filter that quietly selects nothing is a scheduler that watches nothing and reports all clear every morning. Outside the package the crontab entry is removed for a launchd agent (launchd runs a missed job on wake) and `weekly-product-bench.sh` keeps its credential fix while saying at the top that it is no longer scheduled. Honest cost: the scheduled run no longer commits and pushes the result — the criterion reads the working tree either way, but the series survives losing the machine only if someone commits the file, now a line in the weekly rhythm rather than something automatic |
| v0.84.0 ✅ | a probe that never reached the product measured nothing. Bench run 13 scored **build 94% · probes 92% · clean 75%** — the best composite the series has produced, and the first run with all four cases measured. One of its three probe failures was not the product's: case 04's `score-validation-and-evidence-downgrade` failed with `URLError: Connection refused`, so the probe never reached the product, nothing was learned about its behaviour, and it still cost the case a probe (3/3 → 2/3). The cause was in the frame the probes run inside. **The port was the constant 8646** — every probe is a separate process booting its own copy of the product, so a probe could boot onto the port the previous probe's server had not finished releasing, and `proc.terminate()` only asks, it does not wait. **Readiness was a bare TCP connect**, which succeeds against a socket already closing, so the check reported "up" about a server on its way out and the first real call found nobody there. Now: an ephemeral port per probe; readiness that requires an actual HTTP answer (a 404 counts, a transport error does not); a liveness check so a product that dies on import says so instead of timing out for 30s; one retry before believing a refusal, then an `AssertionError` in plain words rather than a raw traceback; and `proc.wait()` after `terminate()` so the port is genuinely back before the probe process exits. Third instance of one shape — run 7's `{}` failures, run 12's error bodies, now this — **the harness charging the product for the harness's own miss**, and each was first read as a product defect |
| v0.83.0 ✅ | an unmeasured case is not a zero, and it is not silent ([ADR-035](adr/035-an-unmeasured-case-is-not-a-zero.md)). v0.82.0 armed the bench loop and it ran the same night — the first bench run a scheduler ever performed. It reported **build 75% · probes 65% · clean 48%** and exited 0, so `avs cadence --notify` printed *no alert: nothing needs a person*. Three of those statements were misleading, for three separate reasons. **Case 04 never ran** — its `pytest -q` did not return within 300s; `run_test_gate` catches `TimeoutExpired` on its own path, but four other callers (build, autopilot, correction, fixpr) reach the runners directly and had no guard, so the exception raised out of everything above it and killed the case an hour in. **The dead case then scored zero** — `_avg` averaged `0.0` for a case with no denominator, which is how "we did not measure this" became "the machine failed at this"; it cost 22 points of probe rate (over the three cases that ran: build 100% · probes 87%), and the launch PRD's **only remaining kill criterion** reads that number against a 50% floor, so a hung subprocess was two more bad weeks away from firing a *capability* verdict about the writer. **And nobody was told** — `product-bench` exits 0 whether or not a case died, the same absence-as-clean-pass shape ADR-033 and ADR-034 each removed one level further out, now one level further in. So a rate averages only over cases that produced its denominator, while a case that ran and built nothing still scores a real `0.0` — a failure, not an absence, pinned by a test so the exclusion cannot become a way to hide bad runs. The denominator travels with the number (`cases_measured`, `cases_total`, `unmeasured` in the saved result; `— over 3 of 4 cases` in the cadence line and the Discord alert), so a later reader of the series can tell 75%-of-four from 75%-of-three. A run that could not measure a case **exits 3** — not because the result is bad but because the harness broke and nothing else will say so — while a merely poor result stays quiet, because a channel that alerts on the benchmark doing its job is a channel nobody reads. A hanging suite now blocks its gate instead of killing the run: `_run_and_classify` converts `TimeoutExpired` into `TestReport(status="error")`, which already blocks APPROVE. The measurement was lying about the **products** too: run 12's case 03 failed two probes with `no error field: {}` while the product answered correctly (`400 {"error": "id must be a base-10 integer: 'abc'"}`) — `urllib` raises `HTTPError` on every 4xx and puts the body on the exception, and the probe frame returned `e.code, {}` without ever calling `e.read()`. Run 7 produced the identical failures and the response then (`2bb4808`, and the `web.yaml` rule beside it) was to tighten the *product* prompt; that rule is good and stays, but it was a fix one layer too low that could never have worked, because the probe could not see the field no matter what the product wrote. **A harness that cannot read the answer will keep reporting that the answer is wrong.** Run 12's recorded numbers are not rewritten — `benchmarks/results/` is a record, not a document — with the recomputed reading and the new denominators' comparability break noted beside them in `HISTORY.md`. Stated rather than left implied: why case 04's suite hangs is still unknown; it is now a blocked task with a named reason instead of a dead run |
| next 🔜 | the v3.0.0 design gate itself: this repo's cycle sits at V3-1/V3-2 met, V3-3 pending — the launch PRD's capability criterion can fire on the next weekly benchmark run, and a human records the decision |

